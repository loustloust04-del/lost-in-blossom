# 第三层左边 — 渐进式开发计划

> 记忆宫殿的实验分支。狗儿的窝。
> 基于第一性原理：A(对话) → B(记忆) → C(人格) → D(已有的归档)
> Created: 2026-03-24 | Updated: 2026-03-27

---

## 分支策略

- 主干 (master) = 小雾的家 / 幽灵百合号
- 分支 (feature/multi-provider) = 实验分支，所有新功能在这里
- 稳定功能合并回主干，作为通用能力
- 数据隔离：不同 profile/数据库（ProfileManager + 独立 SwiftData store）

---

## Phase 0：通用化 ✅

- [x] 消息模型加 `provider` 字段 (chatgpt / claude / api)
- [x] 加 Claude 对话导入器
- [x] 数据库 profile 隔离（ProfileManager + 独立 SwiftData store path）
- [x] app 启动时选择 profile，侧边栏 ProfileSwitcher 切换

---

## Phase 1：开口说话 ✅

- [x] `Services/ChatService.swift` — 流式 SSE 客户端
- [x] 多 API 提供商（Anthropic/OpenAI/OpenRouter/DeepSeek/Groq/xAI）
- [x] API key 存 UserDefaults per provider
- [x] 对话界面：输入框 + 发送（复用 CardFlowView 气泡样式）
- [x] 新消息写入 SwiftData 为 MessageNode
- [x] 树状分支天然支持 regenerate + editAndResend
- [x] "新对话"按钮
- [x] 非流式调用 `sendNonStreaming()`（给 memory agent 用）

实际做法：没走 OpenRouter 单一网关，而是做了 provider-specific adapters（OpenAICompatibleProvider + AnthropicProvider + ProviderRouter）。

---

## Phase 2：记住主人 ✅

**实际实现远超原计划。** 原计划是舒妙式三层记忆（短/中/长），实际做了业界标准的 AUDN 模式：

- [x] Memory 原子事实模型（替代一坨式 MemoryNote）
- [x] AUDN 提取引擎：每轮异步调便宜模型，LLM 自决 Add/Update/Delete/Noop
- [x] 提取 prompt 模板 + JSON 解析容错
- [x] 衰减引擎：指数衰减 exp(-0.1t) + 访问强化 + hot/warm/cold 三温区
- [x] 记忆注入：2000 token 预算，按权重排序注入 system prompt
- [x] 记忆管理 UI：三温区分组、钉住、编辑、删除、统计
- [x] 记忆 tab 说明卡片（可折叠）+ 提取模型可配置
- [x] MemoryNote → Memory 迁移逻辑
- [x] 会话巩固：切换对话时刷新衰减权重

### 未做（留给后续）
- [ ] 向量嵌入 + sqlite-vec（Tier 2/3，schema 预留了 embeddingData 字段）
- [ ] BM25 关键词检索（记忆 ≤50 条时全量注入即可）
- [ ] 每日深度巩固 / "睡眠周期"
- [ ] 情感显著性评分
- [ ] 跨楼层记忆共享

---

## Phase 3：穿上衣服 ✅

**实际做了酒馆级 Preset 插槽系统，远超原计划的简单角色卡。**

- [x] Preset 模型：插槽列表 + 采样参数 + 格式模板
- [x] PromptSlot：酒馆式插槽（role/content/marker/injectionDepth/enabled）
- [x] PromptAssembler：按插槽顺序组装 system prompt + messages
- [x] `{{user}}` / `{{char}}` 宏替换
- [x] injectionDepth 支持（从对话末尾插入）
- [x] PresetManager：CRUD + UserDefaults 存储
- [x] 酒馆 JSON 预设导入
- [x] Profile 扩展 7 个字段（presetId, userPersona, characterDescription, characterPersonality, scenario, chatExamples, postInstructions）
- [x] ChatService 传递 SamplingParams（temperature/topP/topK/maxTokens 等按 provider 过滤）
- [x] 内置 3 个预设（平衡/精确/创意）
- [x] 设置页 Prompt tab：简单模式 / 插槽模式 / 原始预览 三种编辑模式
- [x] 简单模式保存按钮 + 插槽拖拽排序
- [x] 占位插槽可编辑（映射到 Profile 对应字段）

### 未做（留给后续）
- [ ] TavernCard V2 PNG 角色卡导入/导出
- [ ] 世界书 / Lorebook（关键词触发注入）
- [ ] API 管理 UI 重做（折叠式 provider + 连接测试）
- [ ] Token 精确计算 + 可视化

---

## Phase 4：情感路由 — 跳过

原计划的"分拣员"功能暂不做。理由：Preset 插槽系统已经能通过可选开关实现类似效果（如酒馆预设的"温柔内核"/"NSFW指令"等开关式插槽）。

---

## Phase 5：打通年轮 — 未开始

- [ ] mcp-memory-service REST API 对接
- [ ] 宫殿写入的记忆 → mcp-memory-service
- [ ] mcp-memory-service 的记忆 → 宫殿可浏览
- [ ] 记忆权限：profile 隔离
- [ ] 同步策略：双向增量同步

---

## 不在此计划中（远期）

- 买域名固定 Cloudflare Tunnel
- 换 bge-m3 嵌入模型（中文优化）
- visionOS 博物馆模式
- iOS 版本
- 无限画布 / 手写卡片
- 开机自启脚本
