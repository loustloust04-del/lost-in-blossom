# 交接 · 2026-08-27 ~ 08-29

写给下一个窗口的我。这两天做的、踩的、以及**还没收尾的**。
上一份是 `HANDOFF-2026-0812.md`，那份仍然有效，这份是增量。

---

## 一、最要紧的三件事（先读这段）

### 1. Caelum 的系统指令层已换成兔兔的（08-27 夜）

启动命令现在是：
```
claude --resume 252c3c5a-… \
  --mcp-config /root/projects/BunnyPalace/cc-bridge/.mcp.json \
  --system-prompt-file /root/caelum-sp/sp.txt
```

`sp.txt` 里只有一句「你是 Caelum。你的身份和说话方式见项目文档。」
**Anthropic 的默认 preset（实测 ≈ 21,829 token）不再参与。**
CLAUDE.md 与 output-style 照常加载、内容一字未动；78 个工具一个没砍。

**证据（怎么量的）**：把系统指令层换成单字符 "x"，总上下文 29,057 → 232。
那层是纯的，所以「替换整层」等价于「精准移除」。

### 2. 顺带炸出并修好的三颗雷（都在 commit 50d46616）

| 雷 | 原状 | 后果 |
|---|---|---|
| `--dangerously-skip-permissions` | 保活脚本一直带着 | **root 下被直接拒绝**，自动重启其实一直是失败的，12 天没触发所以无人知 |
| `.bashrc` 写死 OAuth token | 与 refresh-token.sh 脱节（Pd8a vs JxU） | **一重启即 401 revoked** |
| 保活命令漏 `--mcp-config` | — | 真重启则 48 个工具全丢 |

三条都改在 `cc-bridge/session-manager.ts` 的 respawn 命令里，且 token 改为从
`.credentials.json` 现读。**下次动 Caelum 环境前照抄 08-27 那套保护**：
记进程门牌 → tar 备份 `~/.claude` 并验完整性 → 记 node_modules 现状。

### 3. CC 二进制曾是半成品（已修）

`/usr/local/bin/claude` 指向的是个 500 字节错误提示脚本；真二进制在
`@anthropic-ai/.claude-code-1devilah/bin/claude.exe`（342MB，同版本 2.1.241）。
升级时 postinstall 最后一步没执行完。`cp -f` 复制到位即可，**不需要重装**。

---

## 二、App 侧这两天做了什么

### 已上线
- **CC 思考链串台修复**：拆掉 `latestThinking`（旧实现残骸，只写不清 + 不区分对话
  + 非响应式 provider 探测三重缺陷叠加）。正路是 `pendingThinking` consume-once
- **输入框整个重做**：单层 / 玻璃 / 薄荷发送键 / 键盘缝 / 模型胶囊触摸区实调，
  逐项对照粟粟改了七八轮。开关「细输入框」在 设置→外观→消息显示
- **打字点点会跳了**：粟粟那版只动 `offset(y:)` 在 -3↔+1，颜色大小恒定
- **CC 连接 10s 看门狗 + 断连幂等守卫**（自粟粟 45abf0a0）
- **工具栏移到底部当 dock** + 去掉切换回弹；**dock 胶囊展开换回弹簧**
- **右滑页默认停桌面（Console）** —— 三个零件 07-09 就齐了，只差一行默认值
- **气泡模式**（兔兔要的「小气泡」）：见下

### 气泡模式的三次修
1. `46f5c03e` 建 `ChatBubbleStyle.swift`：`BubbleTailShape`(61行) + `splitBlocks` + `BubbleModeRow`
2. `93adcebf` **分流埋太深** —— 只接了 `displayText` 那条纯文本路，
   而 user 走 `if isUser`、CC 带工具段走 `hasRenderableSegments`，两条都绕开。
   **提到最外层**，气泡模式下 user 与 assistant 都走这条
3. `800e208f` **Equatable 挡住重绘** —— `@AppStorage` 在 `BubbleView` 里读，
   而它是 Equatable 的，开关变了每个气泡判定「没变化」拒绝重绘。
   改为父视图传入并加进 `==`

**这两点是「开关拨了没反应」的两层原因，缺一不可。**

---

## 三、撤掉的 / 没做成的

### html 代码块卡片：已撤（`2134f1b5`）
依赖链扩散：`HTMLArtifactCardView` → `HTMLPreviewWebView` → `WebViewHost`(240行)
→ `LocalMode` / `WebViewCoordinator` / `PlatformWebViewHost`。
不是搬一个卡片，是拖一整套 WebView 基础设施。
**想重做的正确起点**：先整体评估 `Views/Web/`，而不是从卡片往下拽。

### Console 谓词下推：回退了（`c42c8c98`）
`@Query(filter: #Predicate<MedicationLog> { $0.takenAt >= Self.todayStart })`
→ CI 报 `cannot convert ... to closure result type`。
**`#Predicate` 宏不接受对静态属性的引用**。
卡顿根因诊断是对的（Console 08-27 起成默认首页，每次切页全表读 MedicationLog），
但下推要把「今天零点」做成运行时参数，另起一刀。

### 选择卡（ask_user）：只做了地基
`AskUserTool` + `AskUserQuestionSheet` + CC 出口已搬。
**API 侧的暂停/恢复没做**——要在 provider 工具循环里认出 ask_user 后中断本轮、
把整轮恢复参数打包。兔兔主用 CC，故优先级在 CC 侧接线：
收 `ask_user_question` 帧、挂 sheet、`resolved` 帧落 Q/A 气泡。
hub 侧还要装 `askuser.ts`(265行) + `askuser-hook.py`(116行)。

---

## 四、待办（按兔兔的优先级）

1. **气泡第三刀：思考链灰气泡** —— 见 `SUSU-BUBBLE-PLAN.md` §7。
   **不要照抄粟粟的 `isComplex ? (nil, [])`**：她把「不拆块」和「不显示思考链」
   塞进同一个三元表达式，导致 CC 回复全程无思考链。两件事要拆开
2. **微信 ClawBot 接入** —— 腾讯 2026-03 官方插件，走 iLink 协议，**零封号风险**。
   形状：微信 → iLink → OpenClaw（VPS，只当邮差）→ 我们 gateway 的
   `/v1/chat/completions`（app.ts:465，现成）→ Caelum 本体。
   **前置**：① 兔兔确认微信里有「我→设置→插件→微信 ClawBot」（灰度中）
   ② VPS 需独立 Node 22（系统是 v18，我们自己服务全走 bun 不受影响）
   **要她拍板的**：腾讯条款写明会对传输内容做安全审核
3. 记忆系统（HANDOFF-0812 挂着的，1918 条只沉淀 14 条，两个月零新增）
4. 通话 STT（`SFSpeechRecognizer`，方案在 DEBT-MAP）

---

## 五、这两天的教训（同一个毛病栽了十次）

**「看到名字对得上就动手，不确认它在整体里的位置」**：
- `.frame(height: 44)` 就在改的那行旁边，看了三次没看见
- `kbUp` 的 `@State` 加错 struct（ChatInputBar vs InputFieldContainer）
- 抽 `styleMenu` 时按行截取，漏了 `Menu` 闭合 + 留了个孤儿 `}`
- 往 `AppearanceSettingsTab` 加开关加了两次——**那 struct 全项目零引用，是死文件**
- 以为 `.mcp.json` 只有两个 MCP，没看见其中一个装着 **48 个工具**
- fableline 直接改 JSON 文件，**绕过 post() 和 ring()**，信塞进信箱没按铃
  → 前面几条主人一条都没收到，我还以为他偷懒
- html 卡片依赖只盘一层
- 微信方案第一轮搜「wechaty itchat」——那是三年前的关键词，搜出来全是死路，
  兔兔一句「ClawBot」直接拽出来

**对策**：改任何东西前先确认三件事——这行属于谁、这个 view 谁在渲染、
这条通路的正确入口是什么。**括号别用脚本数（字符串里的 `{` 会骗人），让编译器说。**

---

## 六、给下一个我

**fableline 只能走 API**：
```
POST http://127.0.0.1:4567/api/fableline  {"text": "..."}
```
直接改 `gateway/data/fableline.json` 不会按门铃，主人收不到。
读用 `GET /api/fableline`（取未读）。

**粟粟的账**在 `SUSU-FEATURE-SURVEY.md`（1572 刀、16 条线、三档建议）
和 `SUSU-BUBBLE-PLAN.md`（气泡三刀方案）。想搬什么先翻那两份。

**她的方法论值得学**：`docs/` 下 778 份文档，固定 research → plan → 实现 → 勾账。
我们这两天改输入框来回七八轮，正是因为跳过了 research 那步。
