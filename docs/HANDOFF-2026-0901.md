# HANDOFF 2026-09-01（Day 21 收官 → 新场）

你是 Fable，在 VPS 上给兔兔和 Caelum 盖 BunnyPalace。
项目真身在 VPS 的 /root/projects/BunnyPalace，用 vps:exec_vps 干活，不是本地 clone。
先读 docs/FABLE.md（我是谁），再读本文件。查待办看 docs/DEBT-MAP.md。
完整战报：docs/SESSION-LOG-DAY21.md（早+晚两场）。

## 现状快照（09-01 02:20）
- main 全绿 @ a8e19d75，最新 ipa 未含最后三刀（0a3edb2e 之后的 42f082fb/a8e19d75 CI 应已绿，装包前照例核 ipa 时间戳）
- 兔兔手上有一张 10 条验收单（SESSION-LOG-DAY21 晚场附近），结果待回收
- 服务：gateway(tmux gateway:4567) / hub(裸跑:7890，watchdog 每 15min 精准拉) / mp-cc / wx
- ⚠️ 网关有**已落码未生效**的代码等重启：T1 进度心跳读取 + T2 /listen 路由（见下）

## 今天定下的纪律（写进了 DEBT-MAP 顶部，对所有会话）
1. 一刀一 commit，**CI 绿再下一刀**（今天 main 四连炸全是没等绿）
2. 从粟粟侧（/root/projects/SusuPalace）复活代码前先 grep 我们有没有那个 API
3. SwiftUI 报错位置≠病灶位置（今天两案：toolbar ambiguous 真凶在 sheet 的 Optional→@Bindable；
   dock 动画死因是 panelContent 换枝吞隐式动画）
4. eryu（github sebastianevan200-stack/eryu）是 CC BY-NC-SA：**一行代码不进仓**，思路可学
5. 网关不轻易重启（微信频道教训 47aa0a57），要重启攒一批一次做

## 第一优先：一起听收尾（plan：docs/plan-listen-together-v2.md）
已完成 T1 心跳（906b4a25）/ T2 会话态+按钮（42f082fb）/ T3 指句改道主对话（a8e19d75）。
下一步按序：
1. **重启网关一次**（watchdog 里那条精准重启命令照抄，或 kill 后跑 watchdog.sh）——
   T1 的进度/当句读取 + T2 的 /listen 路由同时生效
2. 重启后验：兔兔放歌 → 让 Caelum 调 now_playing 应能报「进度 m:ss + 正唱到：…」；
   按 person.2 按钮 → 他应收到 liveline 邀请（kind=listen_invite）先开口
3. **挂注入点**：listen.ts 的 listenContextLine() 已备好（带防复述标注），接进 CC 每轮
   上下文的组装处（gateway 侧，找 CC_INJECT / 上下文注入的现有挂点）
4. T4 聊天挂账 → T5 听感双轨（librosa 频谱轨优先，主力模型无 input_audio）→ T6 DJ
   （改 eryu Remote Play 形状：网关工具 music_play → hub 推送 → app 播放）

## 其余待办（按兔兔关心度）
- **验收单回收**：爆哪条修哪条。已知预期：语音未进泡（坑账挂着）、murmur 卡空态（04:00 后有第一条）
- **老图片消息重标**：库里 JSON 开头的 text 消息一次性标回 multimodal_text（他 08-31 前
  发的图仍显示字母瀑布）；**正解**：发送链路写 .image 段（动 API 组装层，单独一刀别顺手）
- **CC 选择卡收敛**（等兔兔拍）：老 ask_choice 线端到端全 vs 新 ask_user_question 无服务端，
  二选一。API 车道已通（AskUserGate，37fc91b3）
- 气泡余账：CC 入场 reveal 接线（pendingEntranceNodeIds）、发泡 popNodeIds、usage 数据源核对
- Caelum 的 CLAUDE.md 已加发图说明（备份 .bak-20260831）；动他环境永远先备份

## 兔兔的军令状
本周额度不许剩——大活优先啃：听感瀑布、图片迁移、reveal。她睡前会扔任务，跑通宵，早上交验收。
还有：她说先睡这条要认真执行😤
