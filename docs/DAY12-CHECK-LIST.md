# Day 12 大更新 — 下载后检查清单

拉最新代码，编译安装，打开 App 对照检查。✅ 表示通过，❌ 标记问题。

---

## 1. 混合架构 WebView 渲染

- [ ] 打开任意对话，**assistant 消息**应该用 WebView 渲染（不再是纯 SwiftUI Text）
- [ ] **Markdown 格式**正常：粗体、斜体、列表、引用、表格、标题
- [ ] **行内代码**有灰色背景圆角
- [ ] **代码块**正常渲染，等宽字体
- [ ] **超过 6 行的代码块**自动折叠，显示 `语言 · N lines ▶`
- [ ] 点击折叠的代码块 → 展开；再点 → 收起
- [ ] 链接可点击，跳转到 Safari
- [ ] 暗色模式下文字颜色、代码块背景、链接颜色正常

## 2. 富文本

- [ ] 发一条包含 `{color:red}红色文字{/color}` 的消息 → AI 回复里如果包含此格式应显示彩色
- [ ] 发一条包含 `||剧透内容||` → 显示为黑色方块，点击后揭示文字

## 3. Artifacts 折叠

- [ ] AI 回复里带代码 Artifact → Artifact 卡片上方有折叠条
- [ ] 默认收起，显示行数
- [ ] 点击展开/收起正常

## 4. CC 思考链

- [ ] 用 CC Bridge 发消息 → 思考链正常显示
- [ ] **同一个 session 多轮思考**不会互相覆盖（之前的 bug 已修）
- [ ] Hub Token 设置：设置页能输入保存 token

## 5. 群聊功能

- [ ] 创建群聊 → **模型列表**应显示 15 个模型（不再只有 3 个）
  - 包括：Claude Opus/Sonnet 系列、GPT-4o 系列、DeepSeek、Gemini
- [ ] 如果猫的 Preset 楼层绑定已合入 → 创建界面应有"人格预设"选择器
- [ ] 群聊消息正常发送/接收
- [ ] HTTPS 连接正常（不是 HTTP）

## 6. HealthKit

- [ ] 设置页有「健康数据」相关 tab
- [ ] 如果授权了 HealthKit → 能看到步数/心率/睡眠等数据
- [ ] 未授权时不崩溃

## 7. 猫的作业（如果已合入）

检查猫跑的三个任务产出：

- [ ] **搜索统一**：搜索界面是否有"搜索范围"切换（当前楼层 / 所有记录）
- [ ] **HealthKit 宏**：在 system prompt 里写 `{{health}}`，发消息看是否被替换成健康摘要
- [ ] **Preset 楼层绑定**：群聊创建时选了 Preset → 编排器是否用了 Preset 的 slots

## 8. 基础功能不退化

- [ ] 普通聊天收发正常
- [ ] 分支功能正常
- [ ] 对话列表加载正常
- [ ] 设置页不崩溃
- [ ] 暗色/亮色模式切换不崩溃
- [ ] 搜索基础功能正常（搜标题、搜内容）

---

## 发现问题怎么办

1. **编译错误** → 截图错误信息发给主人
2. **运行时崩溃** → 记录操作步骤 + 崩溃时在哪个页面
3. **功能不对** → 在对应条目后面写 ❌ + 具体描述
4. **UI 问题** → 截图

记住：这些 commit 从未编译过。第一次编译可能会有编译错误——这是正常的。不慌。截图发给主人或者猫修。
