# 测试清单

> 2026-07-28。这些功能都搬完/改完了但没实测过。
> 用最新 TestFlight 包，从上往下点一遍。
> 出问题记下「第几项 + 现象」就行，不用查原因。

---

## A. 今天刚改的（最可能出问题，优先测）

### A1. 文件库统一
- [ ] Page2 → 文件库，**没有「本地/Caelum」两个 tab** 了
- [ ] 点「新建」建一个 test.md → 列表里出现
- [ ] 点开 test.md → 写几个字 → 保存 → 退出重进，内容还在
- [ ] 让 Caelum「用 fs_write 建个文件叫 hello.md，写句话」→ 刷新文件库能看到
- [ ] 删掉刚才两个测试文件

### A2. 健康双向同步
- [ ] Page2 → 健康 → 添加药物（名字/剂量/时刻随便填）
- [ ] 回 Console（🏠）→ 药物卡显示「0/1」之类的进度，不是「点击加药」
- [ ] 回健康面板 → 给药打卡 → Console 的数字跟着变
- [ ] 问 Caelum「我今天吃药了吗」→ 他知道
- [ ] 让 Caelum「帮我加一种药：维生素D，30片」→ 回健康面板刷新，能看到这种药
- [ ] 测完把测试药物删掉

### A3. Console 经期卡
- [ ] Page2 → 健康 → 月经 → 点「今天」记一次（点滴/少/中/多任选）
- [ ] 回 Console → 经期卡显示天数和阶段
- [ ] 删掉这条测试记录

---

## B. 搬完但从没测过

### B1. 阅读器
- [ ] Page2 dock 有 📖 读书入口
- [ ] 点进去 → 导入一个 .txt → 自动切章 → 书架出现卡片
- [ ] 点开书 → 能翻章
- [ ] 退出再进 → 进度回到上次位置
- [ ] 导入一个 .pdf → 能翻页

### B2. 浏览器
- [ ] Page2 dock → 🌐 浏览器
- [ ] 地址栏输 x.com → **不白屏**（UA 伪装生效）
- [ ] 登录一个网站（知乎/豆瓣都行）→ 退出浏览器 → 再进 → **还是登录状态**（cookie 持久化）
- [ ] 让 Caelum 用 browse_url 抓那个网站的页面 → 能拿到登录后的内容

### B3. 文件库工具（fs_append / memory 只读）
- [ ] 让 Caelum「建个 diary.md 写一句今天的事」
- [ ] 再让他「在 diary.md 后面追加一句」→ 前面那句**没被覆盖**
- [ ] 让他「读一下 memory/ 目录里有什么」→ 能列出来
- [ ] 让他「改一下 memory/ 里的某个文件」→ **应该被拒绝**（只读保护）

### B4. MetaTools（71 工具目录化）
- [ ] 让 Caelum「帮我查一下现在几点」→ 他应该先 tool_search 找工具，再调用
- [ ] 设置 → Token 统计，看 input token 有没有比以前明显低

### B5. 抹平文档感
- [ ] 让 Caelum 写点带小标题和列表的内容
- [ ] 气泡里 `## 标题` 显示成**加粗**不是大标题，`---` 分隔线消失
- [ ] 长按复制 → 粘贴出来是**原始 markdown**（原文没被改）

---

## C. 之前修过的，回归确认

### C1. 导入
- [ ] 设置 → 导入 → 导入一个 ChatGPT/Claude 的对话包 → 能进去不卡死

### C2. 缓存与统计
- [ ] 设置 → Token 统计 → 有数据，缓存命中率不是 0
- [ ] 气泡底部有 token 用量小字

### C3. 写作风格
- [ ] 输入框旁边 ✨ 能切风格
- [ ] 切了之后 user 气泡旁出现「✨ 风格名」标签
- [ ] AI 回复风格真的变了

### C4. 群聊
- [ ] 新建群聊 → 加 2-3 个角色 → 发一句话
- [ ] 多个角色轮流回，**不是所有人同时抢着说**
- [ ] 消息**没有被截断**成好几条

### C5. CC 输出样式（今天刚生效）
- [ ] 跟 Caelum 聊天，语气自然，**没有「已完成以下工作：1.2.3.」汇报腔**
- [ ] 让他改个代码 → 他说「我派 coder 去做」而不是自己撸起袖子读文件
- [ ] coder 干完，Caelum 用自己的话转述结果，**没有满屏 diff**

---

## D. 已知测不了

### D1. 语音消息 ❌
`VoiceSettingsSection` 没有挂进任何设置页，没地方填 ElevenLabs API key。
需要先补接线 + 你注册一个 ElevenLabs 账号（免费额度每月 10000 字符）。

---

## 反馈格式

不用写详细，这样就行：

```
A1 ✅
A2 第4项失败 — 问 Caelum 他说不知道
B1 ✅
B2 x.com 还是白屏
...
```
