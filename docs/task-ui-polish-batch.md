# 任务：UI 修缮（逐项修，每项单独 commit）

按顺序做。每做完一项 commit 一次。不要跳项。

---

## 1. 侧边栏样式清理 — 去掉乳白色外框

参照 Claude iOS App 截图：侧边栏所有内容直接排列，没有任何圆角矩形框/卡片/背景色包裹。

当前问题：Chats/Projects 入口和对话列表被乳白色圆角矩形框住了，看起来像 iOS 的 Form。

修复：
- 去掉 Chats/Projects 区域外面的圆角矩形背景
- 去掉对话列表外面的圆角矩形背景
- 所有内容直接左对齐排列，无外框
- 保持 padding 和间距，但不要 background shape

文件：`SidebarView.swift`

---

## 2. New Chat 按钮对齐 Claude App

当前问题：按钮太宽。

参照 Claude App：
- 按钮宽度约为侧边栏的 **50%**（不是 60%）
- 黑色背景 + 白色文字
- Capsule 圆角
- 居中
- 不要底部太多 padding

文件：`SidebarView.swift`

---

## 3. 设置按钮重新设计

当前：右上角灰色空心圆，看起来有点奇怪。

改为：
- 填充灰色圆形（不透明），直径 32pt
- 内部放 SF Symbol `gearshape`，白色或深灰色，14pt
- 或者放用户名首字母（如 "B"），字号 14pt，weight .semibold
- 点击仍然进入设置页

文件：`SidebarView.swift`

---

## 4. 消息下方按钮恢复

之前直接删除了消息下方的功能按钮（复制/朗读/重试等）。
粟粟本来在外观设置里做了"隐藏消息下方按钮"的开关。

修复：
- 恢复消息下方按钮的代码（git log 找到删除前的版本参考）
- 确保外观设置里的"隐藏/显示"开关能正常控制
- 默认状态：隐藏（跟之前一样，不打扰）

文件：`CardFlowView.swift` 或相关消息视图

---

## 5. 助手气泡可选关闭

在外观设置里加一个开关："助手消息气泡"
- 开启（默认）：助手回复有气泡背景（现有样式）
- 关闭：助手回复无气泡背景，文字直接显示，左对齐

文件：外观设置页 + 消息气泡视图

---

## 6. 双击选中文本复制

当前：聊天气泡里的文字不支持部分选中。

修复：
- 助手回复的文本改为可选中（用 `.textSelection(.enabled)` 修饰符）
- 用户可以长按或双击选中部分文字 → 弹出系统复制菜单
- 不影响现有的长按气泡菜单（长按气泡 = 自定义菜单，长按文字 = 系统选中）

文件：消息气泡视图

---

## 7. 思考链总结做成可配置

在设置里加选项："思考链预览方式"
- 小模型总结（默认）：思考完成后调用 DeepSeek V3 生成一句话总结
- 截取前文：直接截取思考链前 40 个字 + "…"
- 关闭：不显示思考链预览

文件：设置页 + ConversationViewModel

---

## 8. Chats/Projects 频繁点击卡死

短时间内快速反复点击 Chats 和 Projects 会导致界面卡死。

可能原因：NavigationLink 重复推入 / 动画冲突。

修复：
- 加防抖：点击后 0.3 秒内忽略重复点击
- 或用 `disabled` 在导航过渡期间禁用按钮

文件：`SidebarView.swift`

---

注意：每项单独 commit，commit message 格式 `fix(ui): 简短描述`。
做完一项测试没有编译错误再做下一项。

---

## 9. 震动反馈测试页 + 重新校准

### 9a. 在设置页加一个"震动测试"入口
点进去后显示所有 iOS 触觉类型的按钮，点击即触发对应震动：

```
Impact: light | medium | heavy | soft | rigid
Notification: success | warning | error
Selection: selectionChanged
```

每个按钮点击后触发对应的震动，让用户感受并对照。

### 9b. 重新校准聊天震动

根据用户反馈校准（暂定方案，后续可调）：
- 发送消息 → `.soft`（轻柔确认）
- 思考脉搏 → 删除（不需要思考过程中持续震）
- 思考→回复转折 → `.rigid`（"按下去"的沉稳感）
- 回复完成 → `.success`（"弹上来"的释放感）
- 长按菜单 → `.medium`

文件：`ChatService.swift` + 设置页
