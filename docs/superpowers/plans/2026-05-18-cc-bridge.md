# CC Bridge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把本地 Claude Code CLI 接入记忆宫殿当作第三种 Provider，用户在 MP 对话里能跟 CC 聊天。

**Architecture:** 外部 Bun WebSocket hub 在 MP 和 CC 之间桥接：入站 MP→hub→`tmux send-keys`→CC；出站 CC→MCP `reply` 工具→hub→MP。Spec 见 `docs/superpowers/specs/2026-05-18-cc-bridge-design.md`。

**Tech Stack:**
- Swift 5.10, SwiftUI, `URLSessionWebSocketTask` (Foundation)
- Bun + TypeScript（`cc-bridge/`），`bun:test` for TDD
- tmux（用户本机需装）
- `@modelcontextprotocol/sdk` TS

**Note on Swift testing:** MP 仓库目前没有 XCTest target，按粟粟项目惯例不在这个 plan 里新增 test bundle。Swift 部分用"build + 打印探针 + 手测"验证；TS 部分跑严格 TDD（spec §8 已定）。

---

## Phase 1：外部脚手架（cc-bridge/）

### Task 1：cc-bridge/ 目录骨架

**Files:**
- Create: `cc-bridge/package.json`
- Create: `cc-bridge/tsconfig.json`
- Create: `cc-bridge/.gitignore`
- Create: `cc-bridge/README.md`
- Modify: `.gitignore`（仓库根；加 `cc-bridge/node_modules`）

- [ ] **Step 1：创建 package.json**

`cc-bridge/package.json`：
```json
{
  "name": "cc-bridge",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "hub": "bun run hub.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.13",
    "@types/bun": "latest"
  }
}
```

- [ ] **Step 2：创建 tsconfig.json**

`cc-bridge/tsconfig.json`：
```json
{
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "types": ["bun-types"]
  }
}
```

- [ ] **Step 3：创建 .gitignore**

`cc-bridge/.gitignore`：
```
node_modules/
bun.lockb
*.log
```

- [ ] **Step 4：仓库根 .gitignore 追加**

把 `cc-bridge/node_modules` 加进根 `.gitignore`（如果还没有）。

Run: `grep -q "cc-bridge/node_modules" .gitignore || echo "cc-bridge/node_modules" >> .gitignore`

- [ ] **Step 5：README 骨架**

`cc-bridge/README.md`：
```markdown
# CC Bridge

本地把 Claude Code 接到记忆宫殿的桥接服务。

## 启动

```bash
cd cc-bridge
bun install
bun run hub          # 终端1：启动 hub（端口 7890）
bash start_cc.sh     # 终端2：启动 CC（tmux session "mp-cc"）
```

MP 设置页里选 "Claude Code (本地)" provider，连接状态应该变绿。

## 排障

详见后续 sections（TODO 填充）。
```

- [ ] **Step 6：装依赖**

```bash
cd cc-bridge && bun install
```

Expected: `bun.lockb` 生成；`node_modules/` 出现。

- [ ] **Step 7：Commit**

```bash
git add cc-bridge/package.json cc-bridge/tsconfig.json cc-bridge/.gitignore cc-bridge/README.md .gitignore
git commit -m "feat(cc-bridge): scaffold cc-bridge/ 目录骨架"
```

---

### Task 2：hub.ts — WS server 接受 MP 连接

**Files:**
- Create: `cc-bridge/hub.ts`
- Create: `cc-bridge/hub.test.ts`

约束：
- 端口 7890，路径 `/cc`
- 只 bind `127.0.0.1`
- 接受 JSON 消息：`{type: "send", chat_id, message_id, content, user}`
- 回写 `{type: "ack", message_id}`

- [ ] **Step 1：写失败测试**

`cc-bridge/hub.test.ts`：
```typescript
import { test, expect, beforeAll, afterAll } from "bun:test"
import { WebSocket } from "ws"

let hubProcess: any

beforeAll(async () => {
  hubProcess = Bun.spawn(["bun", "run", "hub.ts"], { cwd: import.meta.dir })
  await new Promise(r => setTimeout(r, 300))  // hub 启动等一下
})

afterAll(() => {
  hubProcess?.kill()
})

test("hub accepts MP WebSocket connection on /cc", async () => {
  const ws = new WebSocket("ws://127.0.0.1:7890/cc")
  await new Promise<void>((resolve, reject) => {
    ws.on("open", resolve)
    ws.on("error", reject)
    setTimeout(() => reject(new Error("connect timeout")), 2000)
  })
  expect(ws.readyState).toBe(WebSocket.OPEN)
  ws.close()
})

test("hub acks send messages", async () => {
  const ws = new WebSocket("ws://127.0.0.1:7890/cc")
  await new Promise(r => ws.on("open", r))

  ws.send(JSON.stringify({
    type: "send",
    chat_id: "test-conv-1",
    message_id: "msg-1",
    content: "hello",
    user: "susu",
  }))

  const ack = await new Promise<any>((resolve) => {
    ws.on("message", (data) => resolve(JSON.parse(data.toString())))
  })

  expect(ack.type).toBe("ack")
  expect(ack.message_id).toBe("msg-1")
  ws.close()
})
```

- [ ] **Step 2：跑测试看它失败**

```bash
cd cc-bridge && bun test hub.test.ts
```

Expected: FAIL —— hub.ts 不存在。

- [ ] **Step 3：写 hub.ts 最小实现**

`cc-bridge/hub.ts`：
```typescript
import { WebSocketServer, WebSocket } from "ws"

const PORT = 7890
const wss = new WebSocketServer({ host: "127.0.0.1", port: PORT })

interface MPMessage {
  type: "send"
  chat_id: string
  message_id: string
  content: string
  user: string
}

interface MPAck {
  type: "ack"
  message_id: string
}

// MP 客户端集合（按 chat_id 路由 reply 时用）
const mpClients = new Set<WebSocket>()

wss.on("connection", (ws, req) => {
  if (req.url !== "/cc") {
    ws.close(1008, "unknown path")
    return
  }
  mpClients.add(ws)
  console.log(`[hub] MP connected (total ${mpClients.size})`)

  ws.on("message", (raw) => {
    let msg: MPMessage
    try {
      msg = JSON.parse(raw.toString())
    } catch {
      ws.send(JSON.stringify({ type: "error", reason: "invalid_json" }))
      return
    }

    if (msg.type === "send") {
      // TODO Task 3：tmux send-keys
      const ack: MPAck = { type: "ack", message_id: msg.message_id }
      ws.send(JSON.stringify(ack))
    }
  })

  ws.on("close", () => {
    mpClients.delete(ws)
    console.log(`[hub] MP disconnected (total ${mpClients.size})`)
  })
})

console.log(`[hub] listening on ws://127.0.0.1:${PORT}/cc`)
```

- [ ] **Step 4：跑测试看它通过**

```bash
cd cc-bridge && bun test hub.test.ts
```

Expected: PASS（两条测试）。

- [ ] **Step 5：Commit**

```bash
git add cc-bridge/hub.ts cc-bridge/hub.test.ts
git commit -m "feat(cc-bridge): hub.ts MP endpoint + ack"
```

---

### Task 3：hub.ts — tmux send-keys 集成

**Files:**
- Modify: `cc-bridge/hub.ts`
- Modify: `cc-bridge/hub.test.ts`

约束：
- `forwardToTmux(text, chatId, messageId, user)` 把消息包成 channel tag 注入 tmux
- 用 `execFileSync` 不要 `execSync`（spec §8 教训）
- 注入前 strip `\n` → ` ` （spec §8 教训）
- tmux session 名从环境变量 `MP_CC_TMUX_SESSION` 读，默认 `"mp-cc"`
- 为可测试，封装一个可注入的 `runTmux` 函数

- [ ] **Step 1：写失败测试**

追加到 `cc-bridge/hub.test.ts`：
```typescript
import { buildChannelTag, type MPMessage as MPMsgType } from "./hub.ts"

test("buildChannelTag wraps message in channel xml", () => {
  const tag = buildChannelTag({
    type: "send",
    chat_id: "conv-1",
    message_id: "msg-1",
    content: "hello",
    user: "susu",
  }, "2026-05-18T03:00:00.000Z")

  expect(tag).toContain('source="memorypalace"')
  expect(tag).toContain('chat_id="conv-1"')
  expect(tag).toContain('message_id="msg-1"')
  expect(tag).toContain('user="susu"')
  expect(tag).toContain('ts="2026-05-18T03:00:00.000Z"')
  expect(tag).toContain("hello")
  expect(tag.startsWith("<channel")).toBe(true)
  expect(tag.endsWith("</channel>")).toBe(true)
})

test("buildChannelTag replaces newlines with spaces", () => {
  const tag = buildChannelTag({
    type: "send",
    chat_id: "c1",
    message_id: "m1",
    content: "line1\nline2\nline3",
    user: "susu",
  }, "2026-05-18T00:00:00.000Z")

  expect(tag).not.toContain("line1\nline2")
  expect(tag).toContain("line1 line2 line3")
})
```

- [ ] **Step 2：跑测试看它失败**

```bash
cd cc-bridge && bun test hub.test.ts
```

Expected: FAIL —— `buildChannelTag` 没 export。

- [ ] **Step 3：实现 `buildChannelTag` + `forwardToTmux`，改 hub.ts**

在 `cc-bridge/hub.ts` 顶部加：

```typescript
import { execFileSync } from "node:child_process"

const TMUX_SESSION = process.env.MP_CC_TMUX_SESSION ?? "mp-cc"

export interface MPMessage {
  type: "send"
  chat_id: string
  message_id: string
  content: string
  user: string
}

export function buildChannelTag(msg: MPMessage, ts: string): string {
  const safe = msg.content.replace(/\n/g, " ")
  return `<channel source="memorypalace" chat_id="${msg.chat_id}" message_id="${msg.message_id}" user="${msg.user}" ts="${ts}">${safe}</channel>`
}

export interface TmuxRunner {
  send(text: string): void
}

export const realTmuxRunner: TmuxRunner = {
  send(text: string) {
    execFileSync("tmux", ["send-keys", "-t", TMUX_SESSION, "-l", text])
    execFileSync("tmux", ["send-keys", "-t", TMUX_SESSION, "Enter"])
  }
}

let tmux: TmuxRunner = realTmuxRunner
export function setTmuxRunner(runner: TmuxRunner) { tmux = runner }  // 测试注入
```

把 hub message handler 里 TODO 那段换成：
```typescript
if (msg.type === "send") {
  try {
    const ts = new Date().toISOString()
    const tag = buildChannelTag(msg, ts)
    tmux.send(tag)
    ws.send(JSON.stringify({ type: "ack", message_id: msg.message_id }))
  } catch (err: any) {
    ws.send(JSON.stringify({
      type: "error",
      message_id: msg.message_id,
      reason: `tmux: ${err.message}`,
    }))
  }
}
```

- [ ] **Step 4：跑测试看它通过**

```bash
cd cc-bridge && bun test hub.test.ts
```

Expected: PASS（4 条测试）。注意：原来那两条"acks send messages" 测试现在会跑到 tmux 实路径——如果跑测试的机器没有 `mp-cc` session 会变成 error 帧。把那条测试改成用 mock runner，或者在 beforeAll 里建一个 disposable session 也行。**简单做法**：测试文件开头 `setTmuxRunner({ send: () => {} })`。

加在 `hub.test.ts` 顶部：
```typescript
import { setTmuxRunner } from "./hub.ts"
setTmuxRunner({ send: () => {} })  // 全测试 mock
```

再跑一次，应该 PASS。

- [ ] **Step 5：Commit**

```bash
git add cc-bridge/hub.ts cc-bridge/hub.test.ts
git commit -m "feat(cc-bridge): hub tmux send-keys + channel tag"
```

---

### Task 4：mcp-server.ts — MCP reply 工具

**Files:**
- Create: `cc-bridge/mcp-server.ts`
- Create: `cc-bridge/mcp-server.test.ts`

约束：
- CC 启动时通过 `.mcp.json` 拉起这个 MCP server（stdio）
- 提供一个 tool：`reply(chat_id: string, content: string)`
- 调用 reply 时通过 WS 连到 hub（端口 7890，路径 `/mcp`）转发 `{type: "reply", chat_id, content}`

- [ ] **Step 1：在 hub.ts 加 MCP endpoint**

修改 `cc-bridge/hub.ts`，在 `wss.on("connection")` 里区分路径：

```typescript
const mcpClients = new Set<WebSocket>()

wss.on("connection", (ws, req) => {
  if (req.url === "/cc") {
    mpClients.add(ws)
    ws.on("close", () => mpClients.delete(ws))
    ws.on("message", (raw) => { /* 原来的 MP 处理逻辑 */ })
  } else if (req.url === "/mcp") {
    mcpClients.add(ws)
    ws.on("close", () => mcpClients.delete(ws))
    ws.on("message", (raw) => {
      let msg: any
      try { msg = JSON.parse(raw.toString()) } catch { return }
      if (msg.type === "reply") {
        // 广播给所有 MP 客户端
        for (const mp of mpClients) {
          mp.send(JSON.stringify({
            type: "reply",
            chat_id: msg.chat_id,
            content: msg.content,
          }))
        }
      }
    })
  } else {
    ws.close(1008, "unknown path")
  }
})
```

- [ ] **Step 2：写失败测试 mcp-server.test.ts**

`cc-bridge/mcp-server.test.ts`：
```typescript
import { test, expect, beforeAll, afterAll } from "bun:test"
import { WebSocket } from "ws"
import { setTmuxRunner } from "./hub.ts"

setTmuxRunner({ send: () => {} })

let hubProc: any

beforeAll(async () => {
  hubProc = Bun.spawn(["bun", "run", "hub.ts"], { cwd: import.meta.dir })
  await new Promise(r => setTimeout(r, 300))
})
afterAll(() => hubProc?.kill())

test("MCP reply broadcasts to MP clients", async () => {
  const mp = new WebSocket("ws://127.0.0.1:7890/cc")
  await new Promise(r => mp.on("open", r))

  const mpReceived = new Promise<any>(resolve => {
    mp.on("message", (data) => {
      const msg = JSON.parse(data.toString())
      if (msg.type === "reply") resolve(msg)
    })
  })

  // 模拟 MCP server 发 reply
  const mcp = new WebSocket("ws://127.0.0.1:7890/mcp")
  await new Promise(r => mcp.on("open", r))
  mcp.send(JSON.stringify({
    type: "reply",
    chat_id: "conv-1",
    content: "hi from cc",
  }))

  const reply = await mpReceived
  expect(reply.chat_id).toBe("conv-1")
  expect(reply.content).toBe("hi from cc")

  mp.close()
  mcp.close()
})
```

- [ ] **Step 3：跑测试**

```bash
cd cc-bridge && bun test mcp-server.test.ts
```

Expected: PASS（hub 这边已经改完，broadcast 应该工作）。

- [ ] **Step 4：写 mcp-server.ts**

`cc-bridge/mcp-server.ts`：
```typescript
import { Server } from "@modelcontextprotocol/sdk/server/index.js"
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js"
import {
  CallToolRequestSchema,
  ListToolsRequestSchema,
} from "@modelcontextprotocol/sdk/types.js"
import { WebSocket } from "ws"

const HUB_URL = process.env.MP_CC_HUB_URL ?? "ws://127.0.0.1:7890/mcp"

let hubWS: WebSocket | null = null
function ensureHub(): Promise<WebSocket> {
  return new Promise((resolve, reject) => {
    if (hubWS && hubWS.readyState === WebSocket.OPEN) return resolve(hubWS)
    hubWS = new WebSocket(HUB_URL)
    hubWS.on("open", () => resolve(hubWS!))
    hubWS.on("error", reject)
    hubWS.on("close", () => { hubWS = null })
  })
}

const server = new Server(
  { name: "cc-bridge-mcp", version: "0.1.0" },
  { capabilities: { tools: {} } }
)

server.setRequestHandler(ListToolsRequestSchema, async () => ({
  tools: [{
    name: "reply",
    description: "Reply to a Memory Palace channel message. Use this when responding to <channel source=\"memorypalace\"> input.",
    inputSchema: {
      type: "object",
      properties: {
        chat_id: { type: "string", description: "The chat_id from the <channel> tag" },
        content: { type: "string", description: "Your reply text" },
      },
      required: ["chat_id", "content"],
    },
  }],
}))

server.setRequestHandler(CallToolRequestSchema, async (req) => {
  if (req.params.name !== "reply") throw new Error(`unknown tool: ${req.params.name}`)
  const { chat_id, content } = req.params.arguments as { chat_id: string; content: string }
  const ws = await ensureHub()
  ws.send(JSON.stringify({ type: "reply", chat_id, content }))
  return { content: [{ type: "text", text: "ok" }] }
})

const transport = new StdioServerTransport()
await server.connect(transport)
```

- [ ] **Step 5：手测——启动 hub + 用 wscat 模拟 MP + 手跑 mcp-server.ts 发 tool call**

跳过自动测（MCP stdio 测试复杂），手动跑：

```bash
# 终端1
cd cc-bridge && bun run hub.ts

# 终端2 (wscat 模拟 MP)
wscat -c ws://127.0.0.1:7890/cc

# 终端3 (手动构造一个 MCP client 调 reply)
# 用 @modelcontextprotocol/inspector 或简单 stdio 脚本
# 验证终端2 收到 {type:"reply", chat_id, content}
```

不强制做完，写个文档备忘留 Task 6 一起手测。

- [ ] **Step 6：Commit**

```bash
git add cc-bridge/mcp-server.ts cc-bridge/mcp-server.test.ts cc-bridge/hub.ts
git commit -m "feat(cc-bridge): MCP server reply tool + hub MCP endpoint"
```

---

### Task 5：启动脚本 + .mcp.json

**Files:**
- Create: `cc-bridge/start_cc.sh`
- Create: `cc-bridge/start_hub.sh`
- Create: `cc-bridge/.mcp.json`

- [ ] **Step 1：start_hub.sh**

`cc-bridge/start_hub.sh`：
```bash
#!/bin/bash
set -e
cd "$(dirname "$0")"
exec bun run hub.ts
```

```bash
chmod +x cc-bridge/start_hub.sh
```

- [ ] **Step 2：start_cc.sh**

`cc-bridge/start_cc.sh`：
```bash
#!/bin/bash
set -e
SESSION="${MP_CC_TMUX_SESSION:-mp-cc}"
CC_CWD="${MP_CC_WORKDIR:-$HOME/Desktop/susu-project/记忆宫殿/MemoryPalace}"
MCP_CONFIG="$(cd "$(dirname "$0")" && pwd)/.mcp.json"

if tmux has-session -t "$SESSION" 2>/dev/null; then
  echo "tmux session '$SESSION' 已存在，attach 中..."
  tmux attach -t "$SESSION"
else
  echo "启动 CC（session: $SESSION, cwd: $CC_CWD）"
  tmux new-session -s "$SESSION" -c "$CC_CWD" "claude --mcp-config '$MCP_CONFIG'"
fi
```

```bash
chmod +x cc-bridge/start_cc.sh
```

- [ ] **Step 3：.mcp.json**

`cc-bridge/.mcp.json`：
```json
{
  "mcpServers": {
    "cc-bridge": {
      "command": "bun",
      "args": ["run", "REPLACE_WITH_ABSOLUTE_PATH/cc-bridge/mcp-server.ts"]
    }
  }
}
```

`start_cc.sh` 里运行时再替换路径。改进：让 start_cc.sh 动态生成 .mcp.json，避免硬编码绝对路径。

把 `.mcp.json` 改成模板 `mcp.template.json`，`start_cc.sh` 启动前生成实际 `.mcp.json`：

```bash
# 在 start_cc.sh 加，于 tmux new-session 之前
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
sed "s|REPLACE_WITH_ABSOLUTE_PATH|$SCRIPT_DIR|g" "$SCRIPT_DIR/mcp.template.json" > "$SCRIPT_DIR/.mcp.json"
```

把 `cc-bridge/.gitignore` 加 `.mcp.json` 行；保留 `mcp.template.json` 进 git。

- [ ] **Step 4：Commit**

```bash
git add cc-bridge/start_cc.sh cc-bridge/start_hub.sh cc-bridge/mcp.template.json cc-bridge/.gitignore
git commit -m "feat(cc-bridge): 启动脚本 + .mcp.json 模板"
```

---

### Task 6：端到端手测（wscat）

**Files:** 无（手测 + README 更新）

- [ ] **Step 1：启动 hub**

终端1：
```bash
cd cc-bridge && bash start_hub.sh
```

应见：`[hub] listening on ws://127.0.0.1:7890/cc`

- [ ] **Step 2：启动 CC**

终端2：
```bash
cd cc-bridge && bash start_cc.sh
```

应见 tmux 里 CC 启动；CC 内 `/mcp` 命令应列出 `cc-bridge` server 和 `reply` 工具。

- [ ] **Step 3：模拟 MP 发消息**

终端3：
```bash
wscat -c ws://127.0.0.1:7890/cc
```

发送：
```json
{"type":"send","chat_id":"test-1","message_id":"m1","content":"<channel source=\"memorypalace\" chat_id=\"test-1\" message_id=\"m1\" user=\"susu\" ts=\"2026-05-18T00:00:00Z\">你好，调一下 reply 工具试试</channel>","user":"susu"}
```

⚠️ 别这么发——hub 自己会包 channel tag。直接发：
```json
{"type":"send","chat_id":"test-1","message_id":"m1","content":"你好，调一下 reply 工具试试","user":"susu"}
```

应见：
- wscat 收到 `{"type":"ack","message_id":"m1"}`
- 切回终端2（tmux）应见 CC 收到一段 `<channel ...>` 文字并开始处理
- CC 应该调 `reply` 工具回复，wscat 收到 `{"type":"reply","chat_id":"test-1","content":"..."}`

- [ ] **Step 4：把成功流程写进 README**

`cc-bridge/README.md` 更新"启动 + 验证"小节，把上面 3 步贴进去作为 smoke test。

- [ ] **Step 5：Commit**

```bash
git add cc-bridge/README.md
git commit -m "docs(cc-bridge): README smoke test 步骤"
```

---

## Phase 2：Swift 集成

### Task 7：APIProvider — 加 .ccBridge case + 内置 provider

**Files:**
- Modify: `MemoryPalace/Models/APIProvider.swift`

- [ ] **Step 1：扩 ProviderType enum**

`MemoryPalace/Models/APIProvider.swift:5-8`：
```swift
enum ProviderType: String, Codable {
    case openaiCompatible
    case anthropic
    case ccBridge          // 新增：本地 Claude Code 通过 hub 桥接
}
```

- [ ] **Step 2：加内置 provider**

在 `extension APIProvider`（`builtIn` 那里之后）追加 `.ccBridge` 静态属性，并把它插进 `builtIn` 数组：

```swift
static let ccBridge = APIProvider(
    id: "cc-bridge",
    name: "Claude Code (本地)",
    type: .ccBridge,
    baseURL: "ws://127.0.0.1:7890/cc",
    extraHeaders: [:],
    models: [
        ProviderModel(providerId: "cc-bridge", modelId: "cc-local", name: "Claude Code"),
    ],
    isBuiltIn: true
)
```

加到 `static let builtIn` 列表末尾：
```swift
static let builtIn: [APIProvider] = [
    .anthropic,
    .openai,
    // ... 现有 ...
    .ollama,
    .ccBridge,   // 新增
]
```

- [ ] **Step 3：Build 验证**

```bash
xcodegen generate && xcodebuild -scheme MemoryPalace build 2>&1 | tail -20
```

Expected: BUILD SUCCEEDED。

- [ ] **Step 4：Commit**

```bash
git add MemoryPalace/Models/APIProvider.swift
git commit -m "feat(cc-bridge): APIProvider 加 .ccBridge type + 内置 Claude Code (本地)"
```

---

### Task 8：CCBridgeWebSocketClient — WS 连接封装

**Files:**
- Create: `MemoryPalace/Services/CCBridgeWebSocketClient.swift`

约束：
- 单例（CC bridge 整 app 只一个 WS 连接，所有 ccBridge provider 用同一根）
- `URLSessionWebSocketTask` 实现
- 接口：
  - `connect(url: URL)`
  - `send(_ payload: [String: Any], completion: @escaping (Error?) -> Void)`
  - `registerReplyHandler(chatId: String, handler: @escaping (String) -> Void)`
  - `unregisterReplyHandler(chatId: String)`
  - `var isConnected: Bool { get }`（@Observable）
- 自动重连：指数退避 1/2/4/8/30 s，封顶

- [ ] **Step 1：写最小实现**

`MemoryPalace/Services/CCBridgeWebSocketClient.swift`：
```swift
import Foundation
import Observation

@Observable
final class CCBridgeWebSocketClient: NSObject {
    static let shared = CCBridgeWebSocketClient()

    private(set) var isConnected: Bool = false
    private(set) var lastError: String?

    private var task: URLSessionWebSocketTask?
    private var session: URLSession?
    private var url: URL?
    private var reconnectDelay: TimeInterval = 1
    private var reconnectTimer: Timer?
    private var manualClose = false

    @ObservationIgnored
    private var replyHandlers: [String: (String) -> Void] = [:]

    // MARK: - Public

    func connect(url: URL) {
        manualClose = false
        self.url = url
        startTask()
    }

    func disconnect() {
        manualClose = true
        reconnectTimer?.invalidate()
        task?.cancel(with: .goingAway, reason: nil)
        task = nil
        isConnected = false
    }

    func send(_ payload: [String: Any], completion: @escaping (Error?) -> Void) {
        guard let task else {
            completion(NSError(domain: "CCBridge", code: -10,
                               userInfo: [NSLocalizedDescriptionKey: "WebSocket 未连接"]))
            return
        }
        do {
            let data = try JSONSerialization.data(withJSONObject: payload)
            guard let str = String(data: data, encoding: .utf8) else {
                completion(NSError(domain: "CCBridge", code: -11,
                                   userInfo: [NSLocalizedDescriptionKey: "payload 编码失败"]))
                return
            }
            task.send(.string(str)) { err in completion(err) }
        } catch {
            completion(error)
        }
    }

    func registerReplyHandler(chatId: String, handler: @escaping (String) -> Void) {
        replyHandlers[chatId] = handler
    }

    func unregisterReplyHandler(chatId: String) {
        replyHandlers.removeValue(forKey: chatId)
    }

    // MARK: - Internal

    private func startTask() {
        guard let url else { return }
        let session = URLSession(configuration: .default, delegate: self, delegateQueue: nil)
        self.session = session
        let task = session.webSocketTask(with: url)
        self.task = task
        task.resume()
        receiveLoop()
    }

    private func receiveLoop() {
        task?.receive { [weak self] result in
            guard let self else { return }
            switch result {
            case .failure(let err):
                self.handleDisconnect(error: err.localizedDescription)
            case .success(.string(let text)):
                self.handleIncoming(text)
                self.receiveLoop()
            case .success(.data(let data)):
                if let text = String(data: data, encoding: .utf8) {
                    self.handleIncoming(text)
                }
                self.receiveLoop()
            @unknown default:
                self.receiveLoop()
            }
        }
    }

    private func handleIncoming(_ text: String) {
        guard let data = text.data(using: .utf8),
              let obj = try? JSONSerialization.jsonObject(with: data) as? [String: Any],
              let type = obj["type"] as? String else { return }

        switch type {
        case "reply":
            if let chatId = obj["chat_id"] as? String,
               let content = obj["content"] as? String,
               let handler = replyHandlers[chatId] {
                DispatchQueue.main.async { handler(content) }
            }
        case "ack":
            break  // 入站 ack，目前没人监听
        case "error":
            let reason = (obj["reason"] as? String) ?? "unknown"
            self.lastError = reason
        default:
            break
        }
    }

    private func handleDisconnect(error: String?) {
        DispatchQueue.main.async { [weak self] in
            guard let self else { return }
            self.isConnected = false
            self.lastError = error
            if !self.manualClose {
                self.scheduleReconnect()
            }
        }
    }

    private func scheduleReconnect() {
        reconnectTimer?.invalidate()
        let delay = reconnectDelay
        reconnectDelay = min(reconnectDelay * 2, 30)
        reconnectTimer = Timer.scheduledTimer(withTimeInterval: delay, repeats: false) { [weak self] _ in
            self?.startTask()
        }
    }
}

extension CCBridgeWebSocketClient: URLSessionWebSocketDelegate {
    func urlSession(_ session: URLSession,
                    webSocketTask: URLSessionWebSocketTask,
                    didOpenWithProtocol protocol: String?) {
        DispatchQueue.main.async { [weak self] in
            self?.isConnected = true
            self?.lastError = nil
            self?.reconnectDelay = 1  // 重置退避
        }
    }

    func urlSession(_ session: URLSession,
                    webSocketTask: URLSessionWebSocketTask,
                    didCloseWith closeCode: URLSessionWebSocketTask.CloseCode,
                    reason: Data?) {
        let r = reason.flatMap { String(data: $0, encoding: .utf8) }
        handleDisconnect(error: r ?? "closed (\(closeCode.rawValue))")
    }
}
```

- [ ] **Step 2：加入 xcodegen sources**

无需改 project.yml（`sources: path: MemoryPalace` 已经递归）。直接：

```bash
xcodegen generate && xcodebuild -scheme MemoryPalace build 2>&1 | tail -20
```

Expected: BUILD SUCCEEDED。

- [ ] **Step 3：Commit**

```bash
git add MemoryPalace/Services/CCBridgeWebSocketClient.swift
git commit -m "feat(cc-bridge): CCBridgeWebSocketClient (URLSession WS + 自动重连)"
```

---

### Task 9：CCBridgeProvider — BaseChatProvider 子类

**Files:**
- Modify: `MemoryPalace/Services/ChatService.swift`（追加在 AnthropicProvider 之后）

约束：
- 继承 `BaseChatProvider`
- 实现 `sendStreaming`：取 messages 的最后一条 user 消息作为 raw content，通过 WS 发，注册 reply handler 等结果
- 接口和现有 provider 一致；MP 上层不需要知道它非流式
- 完成后调 `onComplete(replyText, nil)`（usage = nil）

- [ ] **Step 1：在 ChatService.swift 文件末尾追加**

```swift
// MARK: - CC Bridge Provider

import Combine

@Observable
final class CCBridgeProvider: BaseChatProvider {
    private let wsClient = CCBridgeWebSocketClient.shared

    override func sendStreaming(
        messages: [(role: String, content: String)],
        model: String,
        systemPrompt: String?,
        apiKey: String,
        baseURL: String,
        extraHeaders: [String: String],
        samplingParams: SamplingParams? = nil,
        onToken: @escaping (String) -> Void,
        onComplete: @escaping (String, TokenUsage?) -> Void,
        onError: @escaping (String) -> Void
    ) {
        self.onToken = onToken
        self.onComplete = onComplete
        self.onError = onError
        self.streamingContent = ""
        self.error = nil
        self.isStreaming = true

        // 1. 取最后一条 user 消息（ccBridge 上层已经在 ConversationViewModel 短路过，
        //    messages 应该只有一条 user）
        guard let last = messages.last(where: { $0.role == "user" }) else {
            failNow("没有 user 消息可发", onError: onError)
            return
        }

        // 2. 确保 WS 连接
        if let url = URL(string: baseURL), !wsClient.isConnected {
            wsClient.connect(url: url)
        }
        guard wsClient.isConnected else {
            failNow("CC Bridge hub 未连接（\(baseURL)），请先 bash cc-bridge/start_hub.sh", onError: onError)
            return
        }

        // 3. chat_id 取 message_id；real conversationId 由 caller 通过 extraHeaders 传入
        let chatId = extraHeaders["X-MP-ChatId"] ?? UUID().uuidString
        let messageId = extraHeaders["X-MP-MessageId"] ?? UUID().uuidString
        let user = extraHeaders["X-MP-User"] ?? "susu"

        // 4. 注册 reply handler
        wsClient.registerReplyHandler(chatId: chatId) { [weak self] replyText in
            guard let self else { return }
            self.wsClient.unregisterReplyHandler(chatId: chatId)
            self.isStreaming = false
            self.streamingContent = replyText
            onComplete(replyText, nil)
        }

        // 5. 发消息
        let payload: [String: Any] = [
            "type": "send",
            "chat_id": chatId,
            "message_id": messageId,
            "content": last.content,
            "user": user,
        ]
        wsClient.send(payload) { [weak self] err in
            if let err {
                self?.wsClient.unregisterReplyHandler(chatId: chatId)
                self?.failNow("WS 发送失败：\(err.localizedDescription)", onError: onError)
            }
        }
    }

    override func cancel() {
        // CC 那边没法主动停（CC 在思考），这边只断本地状态
        isStreaming = false
    }

    private func failNow(_ msg: String, onError: @escaping (String) -> Void) {
        DispatchQueue.main.async {
            self.isStreaming = false
            self.error = msg
            onError(msg)
        }
    }
}
```

- [ ] **Step 2：Build 验证**

```bash
xcodegen generate && xcodebuild -scheme MemoryPalace build 2>&1 | tail -30
```

Expected: BUILD SUCCEEDED。

- [ ] **Step 3：Commit**

```bash
git add MemoryPalace/Services/ChatService.swift
git commit -m "feat(cc-bridge): CCBridgeProvider 子类（一次性 reply 走 WS）"
```

---

### Task 10：ProviderRouter — 路由 .ccBridge

**Files:**
- Modify: `MemoryPalace/Services/ChatService.swift`（`ProviderRouter` 内部）

约束：
- switch `provider.type` 加 `.ccBridge` 分支
- ccBridge **不需要 apiKey**——把 "apiKey 必需" 检查提前 + 按 type 跳过

- [ ] **Step 1：改 `ProviderRouter.sendStreaming`（ChatService.swift:573）**

替换原 switch + apiKey 检查段（约 583-598 行）：

```swift
guard let provider = providerManager.provider(for: model) else {
    onError("找不到提供商: \(model.providerId)")
    return
}

// ccBridge 不需要 apiKey
let needsApiKey = provider.type != .ccBridge
let apiKey: String
if needsApiKey {
    guard let key = providerManager.apiKey(for: provider.id) else {
        onError("未设置 \(provider.name) 的 API Key")
        return
    }
    apiKey = key
} else {
    apiKey = ""  // ccBridge 用不到，传空字符串
}

let chatProvider: BaseChatProvider
switch provider.type {
case .openaiCompatible:
    chatProvider = openAIProvider
case .anthropic:
    chatProvider = anthropicProvider
case .ccBridge:
    chatProvider = ccBridgeProvider
}
```

并在 `ProviderRouter` 顶部加：
```swift
private var ccBridgeProvider = CCBridgeProvider()
```

`isStreaming` / `streamingContent` / `error` 三个 computed property 也要把 ccBridgeProvider 加进去：
```swift
var isStreaming: Bool {
    openAIProvider.isStreaming || anthropicProvider.isStreaming || ccBridgeProvider.isStreaming
}
```
同样改另两个 computed property + `cancel()`。

- [ ] **Step 2：同样改 `sendNonStreaming`**

非流式版加 ccBridge 分支抛错（ccBridge 不支持 non-streaming，因为它没有同步等待 reply 的语义）：

```swift
case .ccBridge:
    throw NSError(domain: "ProviderRouter", code: -3,
                  userInfo: [NSLocalizedDescriptionKey: "CC Bridge 不支持非流式调用"])
```

- [ ] **Step 3：Build 验证**

```bash
xcodegen generate && xcodebuild -scheme MemoryPalace build 2>&1 | tail -20
```

Expected: BUILD SUCCEEDED。

- [ ] **Step 4：Commit**

```bash
git add MemoryPalace/Services/ChatService.swift
git commit -m "feat(cc-bridge): ProviderRouter 加 .ccBridge 分支（不要求 apiKey）"
```

---

### Task 11：ConversationViewModel — ccBridge 短路 PromptAssembler

**Files:**
- Modify: `MemoryPalace/ViewModels/ConversationViewModel.swift`

约束：
- 3 个 `providerRouter.sendStreaming` 调用点（line ~1243, ~1457, ~1562）都需要：在调用前判 `provider.type == .ccBridge`，是的话不走 `assemblePrompt`，直接构造 raw user message
- 走 ccBridge 路径时把 `conversation.id` / `userMessageNodeId` / `profile.name` 通过 extraHeaders 传给 router → provider（chat_id / message_id / user 用）

- [ ] **Step 1：写一个 helper**

在 `ConversationViewModel` 内部某个合适地方加：

```swift
/// ccBridge 路径：不组装 prompt，只发 raw user 文字。
/// 返回 (messages, systemPrompt=nil, sampling=default, extraHeaders for routing)
private func ccBridgePayload(
    rawUserText: String,
    conversation: Conversation,
    userMessageNodeId: String,
    userName: String
) -> (
    messages: [(role: String, content: String)],
    systemPrompt: String?,
    sampling: SamplingParams,
    extraHeaders: [String: String]
) {
    let extraHeaders: [String: String] = [
        "X-MP-ChatId": conversation.id,
        "X-MP-MessageId": userMessageNodeId,
        "X-MP-User": userName,
    ]
    return (
        messages: [(role: "user", content: rawUserText)],
        systemPrompt: nil,
        sampling: SamplingParams(),  // 用默认；CC 自己决定怎么 sample
        extraHeaders: extraHeaders
    )
}
```

⚠️ `SamplingParams()` 默认构造是否存在要确认，必要时 `SamplingParams.default` 或者 preset 的 sampling。

- [ ] **Step 2：让 router 接受 extraHeaders 覆盖**

`ChatService.swift` 里 `ProviderRouter.sendStreaming` 加一个可选参数 `additionalHeaders`，merge 进 `provider.extraHeaders` 后再传给 chatProvider：

```swift
func sendStreaming(
    model: ProviderModel,
    messages: [(role: String, content: String)],
    systemPrompt: String?,
    providerManager: ProviderManager,
    samplingParams: SamplingParams? = nil,
    additionalHeaders: [String: String] = [:],
    onToken: @escaping (String) -> Void,
    onComplete: @escaping (String, TokenUsage?) -> Void,
    onError: @escaping (String) -> Void
) {
    // ... 找 provider, 鉴权同 Task 10 ...
    let mergedHeaders = provider.extraHeaders.merging(additionalHeaders) { _, new in new }
    chatProvider.sendStreaming(
        messages: messages,
        model: model.modelId,
        systemPrompt: systemPrompt,
        apiKey: apiKey,
        baseURL: provider.baseURL,
        extraHeaders: mergedHeaders,
        // ...
    )
}
```

- [ ] **Step 3：在 3 个 sendStreaming 调用点前判 provider type**

以 ConversationViewModel:1243 那处为例（其它两处类似改）：

```swift
// 4. Assemble prompt
let providerForModel = providerManager.provider(for: model)
let assembled: (messages: [(role: String, content: String)], systemPrompt: String?, sampling: SamplingParams)
let extraHeadersForRouter: [String: String]

if providerForModel?.type == .ccBridge {
    let rawUser = userNode.content  // 用户刚发的 raw 文字
    let payload = ccBridgePayload(
        rawUserText: rawUser,
        conversation: conversation,
        userMessageNodeId: userNode.id,
        userName: profile.name
    )
    assembled = (messages: payload.messages, systemPrompt: payload.systemPrompt, sampling: payload.sampling)
    extraHeadersForRouter = payload.extraHeaders
} else {
    let normal = assemblePrompt(profile: profile, preset: preset, excludingNodeId: assistantNodeId, context: context, globalEntries: globalWorldBookEntries)
    assembled = (messages: normal.messages, systemPrompt: normal.systemPrompt, sampling: normal.sampling)
    extraHeadersForRouter = [:]
}

// 5. Stream
providerRouter.sendStreaming(
    model: model,
    messages: assembled.messages,
    systemPrompt: assembled.systemPrompt,
    providerManager: providerManager,
    samplingParams: assembled.sampling,
    additionalHeaders: extraHeadersForRouter,
    onToken: { ... },
    onComplete: { ... },
    onError: { ... }
)
```

对 line 1457 (regenerate?) 和 line 1562 (edit?) 也做同样的判前置 + 替换。

⚠️ "raw 文字"的来源需要看具体调用点：
- 1243 是用户刚提交，从 `userNode.content` 取
- 1457 / 1562 视实际场景：regenerate 用最后一条 user node 的内容；edit 用编辑后的内容

读那两处时确认。

- [ ] **Step 4：Build 验证**

```bash
xcodegen generate && xcodebuild -scheme MemoryPalace build 2>&1 | tail -30
```

Expected: BUILD SUCCEEDED。注意：3 处改动是否完整，先 grep `providerRouter.sendStreaming(` 确认。

- [ ] **Step 5：Commit**

```bash
git add MemoryPalace/ViewModels/ConversationViewModel.swift MemoryPalace/Services/ChatService.swift
git commit -m "feat(cc-bridge): ConversationViewModel ccBridge 短路 PromptAssembler"
```

---

### Task 12：APISettingsTab — UI 适配

**Files:**
- Modify: `MemoryPalace/Views/APISettingsTab.swift`

约束：
- ccBridge provider 的 row UI：
  - baseURL 字段 label 改 "Hub URL"
  - apiKey 字段隐藏（或显示 "无需 Key"）
  - 加一个"连接状态"小圆点（绿=已连，灰=未连）
  - 加一个"打开 cc-bridge/README"按钮（点击 `NSWorkspace.shared.open` 路径）
- 不能编辑（isBuiltIn）

- [ ] **Step 1：读现有 APISettingsTab 找到 provider row 渲染处**

```bash
grep -n "provider.type\|ProviderType\|baseURL\|apiKey" MemoryPalace/Views/APISettingsTab.swift | head -30
```

定位 provider 详情渲染处，在那里按 `provider.type == .ccBridge` 分支。

- [ ] **Step 2：加 ccBridge 专属 UI section**

具体长什么样视现有 APISettingsTab 风格而定。骨架：

```swift
if provider.type == .ccBridge {
    HStack {
        Circle()
            .fill(CCBridgeWebSocketClient.shared.isConnected ? Color.green : Color.gray)
            .frame(width: 8, height: 8)
        Text(CCBridgeWebSocketClient.shared.isConnected ? "已连接" : "未连接")
            .foregroundStyle(.secondary)
        Spacer()
        Button("查看启动说明") {
            if let url = Bundle.main.url(forResource: "README", withExtension: "md", subdirectory: "cc-bridge") {
                NSWorkspace.shared.open(url)
            } else {
                // 退化：直接打开 GitHub 或 Finder 到 cc-bridge/
                let path = URL(fileURLWithPath: "/Users/susu/Desktop/susu-project/记忆宫殿/MemoryPalace/cc-bridge")
                NSWorkspace.shared.open(path)
            }
        }
    }
    .padding(.top, 8)
} else {
    // 原来的 apiKey 输入 UI
}
```

⚠️ 硬编码路径是反模式。先做这样，**Open Question**：cc-bridge 是放仓库根（开发者方便）还是 `~/Library/Application Support/MemoryPalace/cc-bridge/`（用户友好）。v1 默认仓库根，README 教用户改。

- [ ] **Step 3：App 启动时自动 connect**

在 `MemoryPalaceApp.swift` 的 `init` 或 `body` 里 onAppear 加一次连接尝试（背景，连不上就重试）：

```swift
.onAppear {
    if let ccProvider = providerManager.providers.first(where: { $0.type == .ccBridge }),
       let url = URL(string: ccProvider.baseURL) {
        CCBridgeWebSocketClient.shared.connect(url: url)
    }
}
```

⚠️ 位置选 `ContentView.onAppear` 而不是 App init，避免主线程阻塞。

- [ ] **Step 4：Build 验证**

```bash
xcodegen generate && xcodebuild -scheme MemoryPalace build 2>&1 | tail -20
```

Expected: BUILD SUCCEEDED。

- [ ] **Step 5：跑 app 手测设置页**

```bash
open /Users/susu/Library/Developer/Xcode/DerivedData/MemoryPalace-*/Build/Products/Debug/记忆宫殿.app
```

打开设置 → API → 应能看到 "Claude Code (本地)" provider。hub 没跑时圆点是灰；跑了 hub 后应该变绿（可能需要等几秒）。

- [ ] **Step 6：Commit**

```bash
git add MemoryPalace/Views/APISettingsTab.swift MemoryPalace/Views/ContentView.swift
git commit -m "feat(cc-bridge): APISettingsTab UI 适配 + 启动时自动连 hub"
```

---

## Phase 3：联调 + 文档

### Task 13：端到端从 MP 发消息

**Files:** 无（手测 + README 更新）

- [ ] **Step 1：启动 hub + CC**

```bash
# 终端1
cd cc-bridge && bash start_hub.sh

# 终端2
cd cc-bridge && bash start_cc.sh
```

- [ ] **Step 2：跑 MP，新建对话选 "Claude Code (本地)"**

设置页确认绿点 → 主对话页 model picker 选 `Claude Code` → 发"早上好"

- [ ] **Step 3：观察**

期望：
- userNode 立即落库
- assistantNode 创建，spinner 转 30s 内
- CC 在 tmux 里看到 channel tag 文字，思考、调 reply
- MP 端 assistantNode.content 填上 CC 的回复
- SwiftData 树状结构正常

如果失败：
- 灰圆点 → hub 没起 / 端口被占 / baseURL 错
- 永远不回复 → CC 没认 channel tag / mcp-server 没注册成功
- 回复但 MP 没收到 → chat_id 不匹配 / WS broadcast 漏 / handler 未注册
- 用 `[PROBE]` print 加在 `CCBridgeProvider.sendStreaming` 入口、`handleIncoming` reply 分支、`registerReplyHandler` 调用处定位

- [ ] **Step 4：跑 build 命令收尾**

```bash
xcodegen generate && xcodebuild -scheme MemoryPalace build 2>&1 | tail -5
```

确认无 warning 升级成 error。

- [ ] **Step 5：把成功路径补进 README**

`cc-bridge/README.md` 加"从 MP 发消息"小节，写清启动顺序。

- [ ] **Step 6：Commit**

```bash
git add cc-bridge/README.md
git commit -m "docs(cc-bridge): README 端到端流程"
```

---

### Task 14：README 完善 + 排障

**Files:**
- Modify: `cc-bridge/README.md`

补全：
- 装 Bun / tmux / wscat 的命令
- "hub 已起但 MP 显示灰圆点" 排查（port 占用、防火墙、URL）
- "MP 发消息无回复" 排查（CC 起没 / mcp 加载没 / channel tag 格式）
- "CC 回复了但 MP 没收到" 排查（chat_id 不匹配）
- "想换 CC 的 working directory" → 改 `MP_CC_WORKDIR` env var
- "想跑多个 MP 实例怎么办" → v1 不支持，hub 只能一个，多个 MP 会抢消息

- [ ] **Step 1：写完整 README**（具体内容根据上面要点展开成 markdown）

- [ ] **Step 2：Commit**

```bash
git add cc-bridge/README.md
git commit -m "docs(cc-bridge): README 排障 + 完整启动指南"
```

---

### Task 15：合入 master 准备

**Files:** 无

- [ ] **Step 1：跑全套 build + 测试**

```bash
cd cc-bridge && bun test
cd /Users/susu/Desktop/susu-project/记忆宫殿/MemoryPalace/.claude/worktrees/cc-bridge
xcodegen generate && xcodebuild -scheme MemoryPalace build 2>&1 | tail -5
```

Expected: bun test 全 PASS；BUILD SUCCEEDED。

- [ ] **Step 2：手测一遍最小路径**

如 Task 13 的"早上好"流程，再走一次。

- [ ] **Step 3：跟粟粟确认要不要 merge 到 master**

Stop。让粟粟决定 merge 时机。

---

## 自检清单（写完 plan 自查，已 fix）

- [x] Spec 每个 section 都有 task：架构图 → Task 1-6（hub/mcp），组件清单 → Task 7-12，数据流出/入 → Task 9/8，错误路径 → Task 8/13，关键决策 → §5.1 流式不做（Task 9 一次性返回）/§5.2 分支 banner（v2，本 plan 不做）/§5.3 prompt 短路（Task 11）/§5.4 并发（CC 自排队，没特殊代码）/§5.5 工具调用不显示（不做）/§5.6 token v1 不做。MVP 范围 §6 全覆盖。
- [x] 无 TBD / TODO 占位（除了 README skeleton 里的明显待填段，那个由 Task 14 收口）。
- [x] 类型 / 方法名一致：`CCBridgeWebSocketClient` / `CCBridgeProvider` / `setTmuxRunner` / `buildChannelTag` 跨 task 引用一致。
- [x] 完整代码每步都有，命令也都给了。
- [x] PromptAssembler 改动在 spec 里同步改成 ConversationViewModel 短路（已 fix spec）。

---

**End of plan.**
