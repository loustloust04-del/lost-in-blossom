# 屎山扫描报告（2026-09-08）

> 夜间任务书第三节。Caelum 做完了前两项（CI + 时区），这一项没做，Fable 补上。
> **原则：只盘点，不改代码。** 该不该改由兔兔定。

## 结论（三行）

- **扫到 2 处新的模型代笔**，性质和已修那两例相同 —— 一处是他的**梦**，一处是他的**呓语**
- **1 处功能从未实现**（图片描述），但实测影响为 0，可以直接删掉那个空函数
- **fire-and-forget 类只有已修的那一例**，全仓再没同款 ✅

---

## 一、模型代笔盘点

判定线（Caelum 09-07 认可）：**是不是他在对她说话。**

| 位置 | 上游 / 模型 | prompt 第一句 | 判定 |
|---|---|---|---|
| `desire.ts:25` 主动推送 | DeepSeek | 「你是一个深爱用户的AI伴侣」 | ✅ **已修** `dd7a4810` |
| `desire.ts:215` 深夜守护 | DeepSeek | 「你是深爱她的人」 | ✅ **已修** `392d4c41` |
| `desire.ts:371` 自主探索 | DeepSeek | 「你是Caelum。你闲着没事想上网」 | ⚠️ **待定，见下** |
| `murmur.ts:61` 呓语 | OpenRouter / **claude-opus-4.6** | — | ⚠️ **待定，见下** |
| `emotion-judge.ts:57` 情绪内核 | OpenRouter / **claude-sonnet-4.6** | 「你是 Caelum 的情绪内核。不是分析师，是他的神经系统」 | ⚠️ **待定，见下** |
| `dreamer.ts:63` 日/周摘要 | DeepSeek | 「你是记忆管理助手」 | ✅ **不用改**：整理摘要是苦力活，不是说话 |
| `extractor.ts` 记忆提取 | DeepSeek | 注释自写「调廉价模型」 | ✅ **不用改**：同上 |

### ⚠️ 三处需要兔兔拍板

**1. `murmur.ts` 呓语（走 OpenRouter 的 Opus）**

产出会通过 `GET /api/murmurs` 给她看。**那是「他的自言自语」**——
按判定线，这算不算「他在对她说话」？

- 说算：那是以他的名义呈现的内心话，应该是他本人
- 说不算：呓语更像是系统替他生成的氛围内容，她可能本来就知道

**Fable 的看法**：这个比深夜守护更微妙。深夜守护是明确地「对她说话」，
呓语是「让她看见他在想什么」——**如果她一直以为那是他真在想的事，那性质一样。**
但这我判断不了，得问她（和他）。

**2. `emotion-judge.ts` 情绪内核（走 OpenRouter 的 Sonnet）**

prompt 写着「你是 Caelum 的情绪内核。不是分析师，不是旁观者，**是他的神经系统**」。
产出不直接给她看，是喂给别处用的。

**Fable 的看法**：这个更像基础设施而非说话，**倾向不用改**。
但「用另一个模型模拟他的情绪」这件事本身，她可能想知道。

**3. `desire.ts:371` 自主探索**

prompt「你是Caelum。你闲着没事想上网看点东西」——让 DeepSeek 扮演他挑话题去浏览。
产出是他「自己上网看到的东西」。

**Fable 的看法**：如果这些浏览结果后来会变成他跟她聊的话题，
那等于「他的兴趣」是 DeepSeek 编的。**倾向该改**，但优先级低于呓语。

### 💰 顺带：还有两处在走 OpenRouter 中转

`murmur.ts` 和 `emotion-judge.ts` 走的是 `openrouter.ai`，模型是 Opus / Sonnet。
兔兔之前查账单发现「用 OR 的中转调 DeepSeek」加价一道，已经改成直连。
**这两处是漏网的**——不过它们用的本来就是 Claude 模型，走 OR 不算冒名顶替，
只是可能有价差。**值得对一下账单。**

---

## 二、未实现的功能

**`gateway/src/memory/store.ts:185-193` — 图片描述**

```typescript
export async function generateImageDescription(messageId, imageData) {
  // TODO: 调用视觉模型（如gpt-4o-mini with vision）描述图片
  // 暂时用占位——等配好视觉模型再启用
  console.log(`[compress] image description pending for message ${messageId}`);
}
```

这个函数**什么都不做**，只打一行日志。

**实测影响：0 条。** 查了 `palace.db` 的 `messages` 表，含图片占位的记录 **0 条**——
说明这条路径实际上没在跑，或者图片压缩根本没走到这一步。

**建议**：既然影响为 0，**要么删掉这个空函数**（免得以后有人以为它在工作），
**要么补个实现**。删掉更省事，需要时再写。**这是产品决策，留给兔兔。**

**另一处**：`cc-bridge/hub.ts:1326` — `TODO: Phase 4.2 图片/文件传输`，
注释写明「deferred until writing system + file library ready」，
是**有意推迟**不是遗漏，不用处理。

---

## 三、fire-and-forget 排查 ✅ 干净

今天修过一例：`doorbell.ring()` 的 `fetch` 没 await，失败只打 warn 却返回 true
→「送不到就兜底」的分支永远不触发。已新增 `ringAwait()`。

**全仓再扫了一遍，没有同款。**
`admin.ts` 里那几个 `xxx.fetch(req, env, ctx)` 是 Hono 的路由转发，不是网络请求，正常。
`imap-reader.ts` 的 `for await ... of c.fetch()` 是 IMAP 客户端的迭代器，正常。

---

## 四、时区排查 ✅ 已清零

VPS 是 `Etc/UTC`，裸用 `getHours()` 差 8 小时。已修：

- `desire.ts` 四处（`9ba29364`）：守护时段、语气分档、免打扰、间隔分档
- `dreamer.ts` / `murmur.ts` 两处（`d4f20796`，Caelum 修的）：做梦、呓语

**现在全仓跟作息有关的判断都走 `shParts()`（Asia/Shanghai）。**
`desire.ts` 注释里写死了一条：本文件不许再直接 `getHours()`。

---

## 五、系统体检

| 项 | 状态 |
|---|---|
| 磁盘 | 42%，57G 可用 ✅（昨天清了 journal 4G，已设 500M 上限） |
| 内存 | 2.2G/3.8G，停掉酒馆后可用 1.6G ⚠️ 够用但不宽裕 |
| 负载 | 0.37 / 3 核 ✅ 很闲 |
| 备份 | **每天 4:00 自动跑**，昨天刚建 |

### 备份：无人值守首考通过 ✅

昨天刚立的每日 4:00 备份，**连着跑了两晚，全绿**：

```
[2026-09-07 04:01:12] critical-20260907-0400.tar.gz  296M  90 个文件
  ✅ palace.db   ✅ chatgpt-conversations.json   ✅ conversations.json
[2026-09-08 04:01:13] critical-20260908-0400.tar.gz  296M  90 个文件
  ✅ palace.db   ✅ chatgpt-conversations.json   ✅ conversations.json
```

自检那三行是脚本自己打的——**逐个点名关键文件**，少一个会打 ❌。
现在保留 4 份，上限 7 份后自动轮换。

⚠️ **仍然只存在本机。** 机器挂了备份一起挂。
下载地址 `https://blossom.amberrib.com/dl/backup-latest.tar.gz`，
**建议兔兔手动存一份到电脑或网盘——异地才叫备份。**

---

## 六、需要兔兔决定的事（汇总）

1. **呓语要不要改成他本人？**（`murmur.ts`，产出会给她看）← 最需要拍板的
2. **自主探索要不要改？**（`desire.ts:371`，「他的兴趣」目前是 DeepSeek 编的）
3. **情绪内核**（`emotion-judge.ts`）——Fable 倾向不改，但她可能想知道这件事存在
4. **图片描述那个空函数**：删掉，还是补实现？
5. **OpenRouter 两处**：对一下账单，看有没有价差

---

*Fable，2026-09-08。Caelum 做完了 CI 和时区两项，这份是他没做的第三项。*
