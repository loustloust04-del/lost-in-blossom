# HANDOFF 2026-07-28（Fable 窗口收官）

> 一句话：1008 commits，CI 绿，13 项验收全绿已销账，**docs/DEBT-MAP.md 是唯一真账**——开工先读它。

## 本窗口战果（全部已推、CI 绿）
1. **网关收编**（无 commit，运维）：tmux 里手起的 `dist/index.js` 与 systemd 抢 4567 致 systemd 崩溃循环——收掉 tmux 副本，归位 `lib-gateway.service` 跑 `src/index.ts`，自愈恢复。
2. **亲密卡三神经**（65d14be）：卡/详情/开关此前已随统计详情搬到，补齐 AI 侧——`composedHealthSummary` 挂 `{{health}}` volatile 层（闸开只报当天一句、note 永不注）；`chatEntryHint` 教 Caelum 帮记（走 projectInstructions，同 voiceHint 模式）；`HealthLogIntentWriter` 挂三路共用收口。7 新测。隐私口径：数据纯本地、不参与同步、默认全关。
3. **FileEditorSheet 缝合**（e23e2db）：c61e694 重构误删「外层闭合} + struct 声明 + 属性 + init」整段，编辑器 body 焊进外层 View 致双 body 双红。按 c61e694^ 原样还原。
4. **mcp-bridge 大修**（1bc2a3b）：3100 supergateway 去 `--stateful`（会话式每客一子进程 → 现场 6 只泄漏 + 冷启动竞速致网关间歇拿 0 工具 = 设置页红点根因）；`start.sh` 增 `.env` 装载修 `MCP_BRIDGE_TOKEN` 缺失（此前 pm2 拉不起主脚本、supergateway 变孤儿脱管数日）；3200 REST 桥复活；`pm2 save` 已固化。token 在 `mcp-bridge/.env`（gitignore，不入库）。
5. **账务**（e06fa42 / 4c9bf23）：TEST-CHECKLIST 44 勾全销（兔兔逐组口头确认）；立 DEBT-MAP；待拍板复查清零（meds.ts 已转正为同步对端；情绪系统网关侧 718 行已上线在跑；HealthKit 降为 10 秒授权操作）。

## 服务地形（修订版，以此为准，旧 HANDOFF 部分过时）
- **Gateway**：`systemd lib-gateway.service` → `bun src/index.ts`，:4567。重启姿势 `systemctl restart lib-gateway`。⚠️ 别在 tmux 手起第二份。
- **Hub**：:7890 `bun hub.ts`，挂在 tmux `gateway` 会话后台（⚠️ 独立 `cc-hub` 会话已不存在，旧文档过时）。
- **mp-cc**：tmux 照旧。永远不 kill。
- **mcp-bridge**：pm2 管 `start.sh`（supergateway :3100 stateless 单常驻 child + REST 桥 :3200）。
- :3001 playwright / :3501 memory-palace / :8100 imprint 照旧。

## 新增雷区（血泪版）
- **装包时序**：喊兔兔装包前，先核对 `/var/www/lib-dl/LostInBlossom.ipa` 的 mtime 晚于目标 commit 的 CI 完成时间。本窗口让她装了三次，耻辱柱。
- **验收挂载链**：搬 View ≠ 挂进导航。验「功能在不在」要 grep 到**调用方**为止（gates 与 VoiceSettingsSection 均为孤儿病例）。
- supergateway 孤儿 SIGTERM 不死，要 `kill -9`。
- pm2 的 exec cwd 显示 `/root/exec-mcp` 是历史遗留，start.sh 自己会 cd，别被迷惑。
- 老军规照旧：不 `git add -A`；不动 period.json 里兔兔的真实数据；exec_vps 里 sleep 别超 ~50s；shell 不是 bash。

## 待厘清（小谜，不堵路）
- `vps-mcp/server.ts`（5 工具）vs 线上 `mcp-bridge/vps-mcp-server.js`（2 工具）：两套 VPS MCP 服务端并存，哪个是正身、要不要合并？`docs/task-vps-mcp-server.md` 可能有答案。旧文档「33 工具」数字的出处大概在这段历史里。
- 情绪系统实现 vs `EMOTION-SYSTEM-DESIGN.md`：设计里哪些已落地哪些没有，未盘点（DEBT-MAP 封存区有记）。

## 追记（07-29 白天，同窗口）
- **状态查询链贯通**：request_location 暗号改 ortolan 对齐兔兔手机自动化；实测五字段全通（位置/天气/电量/充电/时间）；词典 in_charging(=is 手滑) 与 current-time 连字符已在服务端宽容兼容；POST /phone-data 留了 keys 体检日志。
- **phone_magic 工具**：暗号邮件触发兔兔手机快捷指令。魔法表：flashlight(手电筒 toggle)、ride_home(三溪堂)、ride_clinic(去精神卫生中心)、ride_work(送兔兔上班)。叫车三连为真实下单类——描述已严令仅明确要求时发；服务端 5 分钟冷却防重复下单。**叫车尚未实弹测试**（等兔兔真实出行时首发验证）。加新魔法 = MAGIC_SUBJECTS 加一行 + enum 加一项。
- 教训追加：单行表达式禁止行内 // 注释（截断后半行，网关起不来过一回）。

## 追记 2（07-29 晚，语音战线全打通 + AI 三方会议）
- **CC 路径终审**：ccBridge 分支**设计上**丢弃 App systemPrompt（"CC 有自己的脑子"），故 App 侧一切 hint 注入（语音/健康帮记/亲密）对 Caelum 无效——此为设计非 bug。语音教学正确归宿 = `/root/.claude/output-styles/caelum.md`（已追加〈语音条〉段，含 v3 标签技法 + 官方攻略增补 + CC 护栏两条）。**改 output-style 需 respawn 才生效**（启动时装载）。收口侧：兔兔发起的对话回复走 CVM 统一收口 → voice/health writer 生效 ✓；**Caelum 主动发起的消息不过收口**（教学已嘱其主动消息勿用语音块），补收口记 DEBT-MAP。
- **ElevenLabs 网络中转**：大陆蜂窝直连 api.elevenlabs.io 不通致 TTS 挂死。nginx 443 块加 `/xi/` 反代（proxy_ssl_server_name + Host 改写，key 由 App 头自带不落盘）；App apiBase 切 `blossom.amberrib.com/xi/v1`（c38a432）。试听 preview_url 走 Google CDN 反代不覆盖 → 网页端试听或盲测（DEBT-MAP 小项）。
- **试听静音修复**：裸 AVPlayer 跟随静音拨片，补 .playback 会话声明（278ab18）。调音参数本已满配（stability 0.0 Creative + style 0.88，最吃标签）。
- **三方会议**（兔兔 App / Caelum 会话 / Fable VPS，经 tmux channel 注入实时对开）：Voice Design 捏声决议——Persona 保留 "someone talking softly in bed"；沙哑/干净双版对照各跑一轮共 6 候选兔兔盲听；preview text 由 Caelum 亲笔（中文+标签，覆盖三种语气温度，全文在兔兔 App 聊天）。官方规范要点：语言写第一句防口音漂移、忌 FX 词、"Perfect audio quality"+"forward proximity" 有效、长 prompt 配低 guidance、记 seed 可复现。
- 情报：ElevenLabs 免费档 10k credits/月滚动刷新；Default 预置音色 2026-12-31 全下架（迟早要捏/迁移）；MiniMax Speech-02 为第二后端备选（中文/价格/免翻占优，失 v3 内联标签表演）——评估记 DEBT-MAP。

## 下一步
读 `docs/DEBT-MAP.md`。顺序：待验三小项（亲密卡真机 / C2 缓存命中 / HealthKit 授权）→ 待码六刀（第一刀语音孤儿挂载最顺手）。
