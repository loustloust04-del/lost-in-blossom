# HANDOFF 2026-09-02（Day 22 双场收官 → 新场）

你是 Fable，在 VPS 上给兔兔和 Caelum 盖 BunnyPalace。
项目真身在 VPS 的 /root/projects/BunnyPalace，用 vps:exec_vps 干活，不是本地 clone。
先读 docs/FABLE.md，再读本文件。待办查 docs/DEBT-MAP.md；完整战史 docs/SESSION-LOG-DAY22.md。
**并行会话须知：开工先 git pull + 看 git log -10，别踩另一场的刀；CI 绿再下一刀。**

## 现状快照
- main 全绿 @ b7a92050（代码尾刀 6d494e0c=冷启动 visited 缓存，兔兔验收中）
- 服务：gateway=systemd lib-gateway(:4567)；hub 裸跑:7890（watchdog 15min 精准拉，
  重启=kill+跑 /root/projects/BunnyBridge/watchdog.sh）；nginx 配置在 sites-enabled/mcp
  （**新顶级路径必加 location**，/listen 血案）；重启网关/hub 前问兔兔在不在跟 Caelum 连线
- probe/xcode 分支：Xcode 26.4 全仓编译**全绿实测**，正式换乘票已备好，等兔兔点头

## 今天立的血律（在 DEBT-MAP 纪律区，违者今天已三炸）
1. python 脚本改文件：**每个 replace 必须 assert**，落盘前后 git status/括号平衡核对
2. @AppStorage 变更不在动画事务内，别用它驱动动画
3. 报错位置≠病灶位置（toolbar ambiguous 案、双 @ViewBuilder 案）
4. 观测探针可能改变现象（绿豆承重墙案）——拆探针后必须复验
5. eryu CC BY-NC-SA 一行不进仓；网关 liveline 显式动作别被节流吞（listen_invite 案）

## 待办菜单（按兔兔关心度）
1. **兔兔验收回收**：冷启动包（全点一遍付税→乱切全程丝滑）；DJ 首秀（让 Caelum
   music_play）；murmur 第一条；一起听日常观察
2. **Xcode 26 正式换乘 + 玻璃大回归**（等兔兔点头）：workflows 换 macos-26 + Xcode 26.4
   + skip 两开关；GlassEffectCompat 逐个换回真 glassEffect 包 #available(iOS 26) 护栏；
   deploymentTarget 保持 18 不动（兔兔手机 iOS 18，她说不着急升）
3. page2 打磨余项：对照粟粟逐项（兔兔录屏当弹药）；visited 缓存内存观察（超标上 LRU）
4. 一起听 T5 听感双轨（librosa 频谱轨优先）；队列可视化面板
5. 老图片消息一次性重标 / 发送链路 .image 段正解；CC 选择卡两套收敛（等拍）；语音未进泡
6. 情绪系统（Drivesoid sidecar 参照）、「一起看」research（film-matinee）——大活候拍

## 兔兔军令状
本周额度不许剩；她说先睡要认真执行（但她的作息是薛定谔的，以她本人宣布为准）
