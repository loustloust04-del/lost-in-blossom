# 记忆宫殿 (Memory Palace) — 总设计文档

> 最后更新：2026-04-16

---

## 项目概述

macOS + iOS 原生 AI 对话浏览器 + 聊天客户端。导入历史对话（ChatGPT/Claude），在宫殿里跟 AI 实时聊天，AI 会记住你，你控制它怎么说话。

**七根柱子**：A(对话) ✅ → B(记忆) ✅ → C(人格) ✅ → D(归档) ✅ → E(角色卡+世界书) ✅ → F(工具抽屉) ✅ → G(贴纸) ✅

## 技术栈

- SwiftUI + SwiftData, macOS 14+ / iOS 17+
- Swift 5.10, Xcode 16+
- MarkdownUI v2.4.1（assistant 气泡 Markdown 渲染）
- xcodegen 生成 `.xcodeproj`（`project.yml` 为配置源）
- Bundle ID: `com.susu.MemoryPalace`，产品名: `记忆宫殿`

```bash
cd "/Users/susu/Desktop/susu-project/记忆宫殿/MemoryPalace"
xcodegen generate && xcodebuild -scheme MemoryPalace build
```

## 代码规模

- **27,000 行** Swift，**73 个文件**
- 205,981 MessageNode / 1,733 对话

## 文件结构

```
MemoryPalace/
├── project.yml                              # xcodegen 配置
├── 记忆宫殿-这才是真的总设计文档！.md          # 本文件
├── CLAUDE.md                                # Claude Code 指令
├── docs/
│   ├── PROJECT_ROADMAP.md                   # 唯一待办来源
│   ├── plan-v1-testflight.md                # TestFlight 发布计划
│   ├── research-prompts.md                  # 可丢给别的 AI 的调研 prompt
│   ├── design-dna.json                      # 设计 token 定义
│   ├── dev-workflow.md                      # 开发流程
│   └── archive/                             # 已完成功能的 research/plan
│
├── MemoryPalace/
│   ├── MemoryPalaceApp.swift                # App 入口, Profile, ProfileManager, PresetManager, WindowConfigurator
│   │
│   ├── Models/                              # 16 个数据模型
│   │   ├── Conversation.swift               # Conversation, MessageNode, UserCard
│   │   ├── Folder.swift                     # Folder, FavoriteItem, ImportRecord, ImportConversationChange
│   │   ├── APIProvider.swift                # ProviderType, APIProvider, ProviderModel, ProviderManager
│   │   ├── Memory.swift                     # Memory 原子事实模型（AUDN）
│   │   ├── MemoryNote.swift                 # [遗留] 旧记忆模型，仅用于迁移
│   │   ├── Preset.swift                     # PromptSlot, SamplingParams, Preset（酒馆式插槽系统）
│   │   ├── CharacterCard.swift              # TavernCard V2/V3 角色卡模型
│   │   ├── WorldBook.swift                  # 世界书条目模型
│   │   ├── GlobalWorldBook.swift            # 全局世界书管理
│   │   ├── RegexScript.swift                # 正则脚本模型
│   │   ├── RightPanelPlugin.swift           # 右栏插件注册制
│   │   ├── StickerAsset.swift               # 贴纸资产模型
│   │   └── PlacedSticker.swift              # 已放置贴纸模型
│   │
│   ├── ViewModels/
│   │   ├── ConversationViewModel.swift      # 树遍历、分支冒泡、prompt 集成、AUDN 提取
│   │   └── StickerViewModel.swift           # 贴纸画布状态管理
│   │
│   ├── Views/                               # 34 个视图文件
│   │   ├── ContentView.swift                # NavigationSplitView 主框架（macOS + iOS 双布局）
│   │   ├── SidebarView.swift                # 侧边栏：搜索、文件夹、对话列表
│   │   ├── CardFlowView.swift               # 气泡对话流、分支选择器、输入栏、模型选择器
│   │   ├── CalendarPanelView.swift          # 日历面板
│   │   ├── MemoryPanelView.swift            # 右栏容器 + 工具抽屉路由
│   │   ├── ToolBarView.swift                # 工具抽屉 bar（橡皮绳动画）
│   │   │
│   │   ├── SettingsView.swift               # 设置页路由（225 行，纯路由）
│   │   ├── GeneralSettingsTab.swift         # 设置 - 通用
│   │   ├── AppearanceSettingsTab.swift      # 设置 - 外观
│   │   ├── PersonaSettingsTab.swift         # 设置 - Prompt（5 种编辑模式 + 采样参数）
│   │   ├── APISettingsTab.swift             # 设置 - API（+ ProviderCard + AddProviderSheet）
│   │   ├── MemorySettingsTab.swift          # 设置 - 记忆
│   │   ├── RegexSettingsTab.swift           # 设置 - 正则
│   │   ├── StickerSettingsTab.swift         # 设置 - 贴纸
│   │   ├── DataSettingsTab.swift            # 设置 - 导入导出
│   │   │
│   │   ├── ImportView.swift                 # JSON 导入界面
│   │   ├── ImportHistoryView.swift          # 导入历史管理
│   │   ├── ExportOptionsSheet.swift         # 导出选项弹窗
│   │   │
│   │   ├── WorldBookPanelView.swift         # 世界书面板
│   │   ├── CardLibraryPanelView.swift       # 角色卡库面板
│   │   ├── CharacterCardEditor.swift        # 角色卡编辑器
│   │   ├── RegexScriptEditor.swift          # 正则脚本编辑器
│   │   │
│   │   ├── StickerCanvasLayer.swift         # 贴纸画布层
│   │   ├── StickerGestureOverlay.swift      # 贴纸手势处理
│   │   ├── StickerView.swift                # 单个贴纸渲染
│   │   ├── StickerLibraryView.swift         # 贴纸库
│   │   ├── StickerImportSheet.swift         # 贴纸导入
│   │   ├── StickerToolbar.swift             # 贴纸工具栏
│   │   ├── StickerStyleSheet.swift          # 贴纸样式面板
│   │   ├── StickerKeyboardPanel.swift       # iOS 贴纸键盘面板
│   │   ├── DrawingBoardSheet.swift          # 画板
│   │   ├── NoteStickerEditor.swift          # 便签贴纸编辑
│   │   ├── NoteExportView.swift             # 便签导出
│   │   └── IOSPromptTextView.swift          # iOS Prompt UIKit 文本编辑器
│   │
│   ├── Services/                            # 17 个服务
│   │   ├── ChatService.swift                # BaseChatProvider + OpenAI/Anthropic + ProviderRouter
│   │   ├── PromptAssembler.swift            # 按插槽顺序组装 prompt + 宏替换
│   │   ├── PromptPostProcessor.swift        # 正则后处理器
│   │   ├── MemoryService.swift              # MemoryStore + DecayEngine + MemoryInjector + MemoryExtractor
│   │   ├── ConversationImporter.swift       # ChatGPT JSON 导入
│   │   ├── ClaudeImporter.swift             # Claude 对话导入
│   │   ├── ImportSupport.swift              # 导入辅助
│   │   ├── MarkdownExporter.swift           # Markdown 导出
│   │   ├── SearchService.swift              # 搜索服务（标题+内容+高级筛选）
│   │   ├── SubjectLifter.swift              # 主题提取
│   │   ├── TavernCardParser.swift           # TavernCard V2/V3 解析器（JSON + PNG）
│   │   ├── WorldBookScanner.swift           # 世界书关键词扫描 + PromptAssembler 注入
│   │   ├── StickerFileManager.swift         # 贴纸文件管理
│   │   ├── StickerBorderRenderer.swift      # 贴纸描边渲染
│   │   ├── StickerFilterRenderer.swift      # 贴纸滤镜渲染
│   │   ├── StickerPackExporter.swift        # 贴纸包导出
│   │   └── StickerPackImporter.swift        # 贴纸包导入
│   │
│   └── Utils/
│       ├── Theme.swift                      # 配色常量 + 字号常量（暖奶白 + 浅灰薄荷）
│       ├── MarkdownTheme.swift              # MarkdownUI 自定义主题
│       ├── FontManager.swift                # 字体管理
│       └── ContentCleaner.swift             # ChatGPT 引用/实体标记清理（NSCache）
```

## 核心架构

### 楼层系统 (Profile)
- 每个楼层 = 一个独立数据库（SwiftData store）+ 一套配置
- Profile 存 UserDefaults（JSON），包含：基础信息、presetId
- ProfileSwitcher 在侧边栏切换楼层
- 内置两个楼层：幽灵百合号（小雾）、第三层左边（小克）

### 多 API 提供商 (ProviderManager)
- 6 家内置：Anthropic、OpenAI、OpenRouter、DeepSeek、Groq、xAI
- 支持自定义提供商（OpenAI Compatible 格式）
- 两种 provider 类型：OpenAI Compatible、Anthropic Messages API
- ProviderRouter 根据 model.providerId 路由到正确的 provider
- 支持流式 (sendStreaming) 和非流式 (sendNonStreaming) 调用

### 记忆系统 (AUDN)
- **Memory 模型**：原子事实（content + category + keywords + decayWeight + accessCount...）
- **AUDN 提取**：每轮对话后异步调便宜模型，LLM 通过 JSON 指令执行 Add/Update/Delete/Noop
- **衰减引擎**：指数衰减 exp(-0.1t)，访问时强化 +0.2，三温区（hot≥0.3 / warm≥0.05 / cold<0.05）
- **注入**：hot 记忆按权重排序注入 system prompt，2000 token 预算
- **管理 UI**：设置页"记忆"tab，三温区分组显示，钉住/编辑/删除

### Preset 插槽系统（酒馆架构）
- **Preset** = 有序的 PromptSlot 列表 + SamplingParams
- **PromptSlot**：name, role(system/user/assistant), content, isMarker, injectionDepth, isEnabled
- **PromptAssembler**：遍历插槽列表 → marker 用 Profile 数据填充 → `{{user}}`/`{{char}}` 宏替换 → injectionDepth 插入到对话历史指定位置
- **PromptPostProcessor**：正则脚本后处理（对齐酒馆引擎）
- **SamplingParams**：temperature, topP, topK, maxTokens, frequencyPenalty, presencePenalty, contextDepth
- **五种编辑模式**：简单（5 个文本框）/ 插槽（完整列表+拖拽）/ 原始（JSON）/ 组装预览 / 最终请求预览
- **酒馆预设导入**：解析 SillyTavern JSON 格式

### 角色卡 + 世界书
- **TavernCard V2/V3**：JSON + PNG 两种导入格式，角色卡库 + 编辑器
- **世界书**：关键词触发注入，WorldBookScanner 扫描对话内容匹配条目
- **全局世界书**：跨楼层共享的世界书，GlobalWorldBookManager 管理
- **正则脚本**：预设/角色卡/手动三来源，引擎对齐酒馆

### 工具抽屉（右栏）
- 插件注册制（RightPanelPlugin）
- ToolBarView：一条 bar + 长按网格展开 + 橡皮绳动画
- 当前工具：日历、记忆面板、世界书、角色卡库、贴纸

### 贴纸系统
- StickerAsset：图片贴纸资产（描边 + 滤镜 + 缩略图）
- PlacedSticker：画布上的贴纸实例（位置/旋转/缩放）
- 便签贴纸、画板贴纸
- iOS 贴纸键盘面板
- 贴纸包导入/导出

### 树遍历 (ConversationViewModel)
- 从 root（parentId==nil）沿 children 向下走，遇到分支按 branchChoices 选择
- **分支冒泡**: system/tool 不可见节点的分支信息冒泡到可见节点
- **effectiveChildrenMap**: 从 parentId 反向重建父子关系
- **后台线程加载**: fetch + 树构建在后台线程（独立 ModelContext + 纯值类型 NodeInfo）

### 设置页架构
- SettingsView（225 行）：纯路由，iOS 用 NavigationStack + List，macOS 用自定义 tab bar
- 8 个独立 tab 文件，每个管自己的 @State
- iOS 每个 tab 有独立的全屏 push 页面

### 配色 (Theme)
暖奶白 `#FFFBF6` 为主，浅灰薄荷 `#E7EEEC` 为辅，深棕色文字。**不要蓝色、不要黄色**。

---

## 数据规模

- 20 万+ MessageNode
- 任何 FetchDescriptor 必须加 predicate，**禁止无条件全量 fetch MessageNode**
- ContentCleaner.clean() 的 cacheKey 必须传 node.id

---

## 待办

见 `docs/PROJECT_ROADMAP.md`（唯一来源）。
